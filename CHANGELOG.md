# Changelog

All notable changes to `rust-wallet-app` (Bitcoin wallet MVP).

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) with two extensions:

- `[user-facing]` / `[internal]` tags distinguish client-visible changes from engineering work
- User Stories section at the bottom shows what's playaround-able today

> **Tag legend:** `[user-facing]` items are visible to clients / external users (new public API, new behavior, new threat-model surface). `[internal]` items are engineering work invisible to users (refactor, audit, defense-in-depth that doesn't change public API).

[Unreleased]: https://github.com/nhitranbtc/blockchain-sdk/compare/v0.2.0...HEAD
[v0.2.0]: https://github.com/nhitranbtc/blockchain-sdk/compare/v0.1.0...v0.2.0
[v0.1.0]: https://github.com/nhitranbtc/blockchain-sdk/releases/tag/v0.1.0

## [Unreleased]

- **`[internal]`** `wallet-desktop/lib/core/ffi/ffi_exception.dart` + `wallet-desktop/lib/core/wallet_core.dart` + `wallet-desktop/lib/features/wallet_send/send_screen.dart` + `wallet-desktop/test/core/ffi/ffi_exception_test.dart` + `wallet-desktop/test/unit/send_screen_test.dart` (Issue #265, PR pending, SHA pending, follow-up Issue #266) — **SendScreen per-op copy + thread-local `lastError` surface**. SendScreen catch handler previously rendered `userMessageForFfiException(e)` (the kind-only fallback) regardless of which FFI op actually failed — `esplora_client_new` builder failure (rustls 0.23 crypto-provider mismatch) surfaced as the misleading "Invalid recovery phrase — please re-enter." copy even though SendScreen has no recovery-phrase field in v0.2.x. **Fix**: new `userMessageForFfiExceptionWithOp(FfiException)` keys on `error.op` (4 arms: `esplora_client_new` / `wallet_show` / `wallet_load` / `wallet_send`; default falls through to the kind-only helper for legacy callers). `FfiException` gains optional `lastError` field (populated in `WalletCore._ffiError` from Rust thread-local `ffi_last_error_message`); `toString` excludes both `lastError` + `messageForDebug` (L12 CRITICAL #2). SendScreen render uses `BtcLogFilter.redact` before any Text widget receives `error.lastError` (Rust sanitizer only strips NUL bytes — Issue #242 — so BIP-39 sequences can reach the Dart side; the scrub is the load-bearing defense). 12 new tests (8 unit on `FfiException`/`userMessageForFfiExceptionWithOp` + 3 string-level on SendScreen wiring + 1 12-word BIP-39-shaped redaction check across `toString` + both copy functions). L12 critical-tier review (compass:code-reviewer + compass:security-auditor): 1 MEDIUM applied (try/catch around `ffiLastErrorMessage` lookup — older Rust crate without the symbol throws `ArgumentError`, must not propagate past `_ffiError`), 1 MEDIUM applied (SendScreen now renders `lastError` so it's not dead code), 1 LOW applied (3 `developer.log` calls now scrub via `BtcLogFilter.redact` at the call site). `flutter test` clean on my files (54/54 pass; 15 pre-existing failures in `wallet_create_screen_test` / `wallet_detail_screen_test` / `wallet_list_screen_test` etc. are not in this diff — separate drift). `dart analyze` clean on my new lines (12 issues all in pre-existing untouched code). **Out of scope** (follow-up Issue #266 — Rust-side): root-cause fix for the rustls 0.23 crypto-provider mismatch (`EsploraClient::new` needs explicit `rustls::crypto::aws_lc_rs::default_provider().install_default()` before constructing the reqwest client). Issue #266 unblocks both this issue AND the SyncFailed banner from #263. **Pre-flight gotcha (operator-driven)**: rebuild the cdylib via `bash wallet-desktop/tool/build_native.sh` before launching the UI after any Rust ABI change — Dart caches FFI symbols at process start. This PR is Dart-only — hot-reload picks up without rebuild.

- **`[internal]`** `rust-wallet-app/crates/bitcoin-wallet-core/src/ffi/wallet_ops.rs` + `wallet-desktop/lib/core/ffi/ffi_enums.dart` + `wallet-desktop/lib/core/ffi/wallet_ops_bindings.dart` + `wallet-desktop/lib/core/wallet_core.dart` + `wallet-desktop/lib/core/btc/models/wallet_detail.dart` + `wallet-desktop/lib/widgets/balance_card.dart` + `wallet-desktop/lib/features/wallet_detail/wallet_detail_screen.dart` + `wallet-desktop/test/unit/wallet_detail_screen_test.dart` (Issue #263, sub-issue of #261, PR pending, SHA pending) — distinct "sync failed" UX state on the detail screen + **operator-visible failure diagnostic**. Closes the silent-swallow gap from PR #262 where every Esplora failure mode (URL parse / client construction / tokio runtime build / `address_utxos` HTTP / per-UTXO MAX_MONEY cap / `checked_add` overflow) surfaced as `balance_sat: 0` with the same "no funds yet" hint — operator couldn't distinguish a fresh empty wallet from a broken Esplora sync, and had no way to see WHY sync failed. **ABI**: new `#[repr(u8)]` enum `WalletSyncStatus { Synced = 0, EmptyWallet = 1, SyncFailed = 2 }` (Rust) ↔ `FfiSyncStatus` (Dart, with defensive `unknown(255)` for ABI drift). `wallet_show` gains `out_sync_status: *mut u8` (13th out-param) — values MUST match the Dart `fromCode` factory. **Mapping**: empty / null `esplora_url` → `EmptyWallet` (legacy v0.2.0 path); successful sync (including legitimately empty wallet with 0 confirmed UTXOs) → `Synced`; every failure arm flips to `SyncFailed`. **Diagnostic surface (C1 fix)**: existing Rust `ffi_last_error_message()` (thread-local `CString` from `set_last_error`) is now bound to Dart; `WalletCore.showWallet` reads it immediately after `walletShow` returns (next FFI call invalidates the borrow) and populates `WalletDetail.lastError`. `BalanceCard` renders the message in the red banner body so the operator sees `wallet_show esplora client: <err>` etc. instead of generic copy. **UX**: `BalanceCard` switches 3-way — `Synced` = balance headline only; `EmptyWallet` = existing "syncs on unlock" hint; `SyncFailed` (and `unknown`) = red `errorContainer` banner + `lastError` body + Retry button wired to existing `_showReUnlockDialog` (idempotent — re-invokes `walletShow`). `Retry` button gated on `_running` (L12 review M2 — prevents stacked dialogs from double-tap). `WalletDetail.syncStatus` defaults to `FfiSyncStatus.emptyWallet` for back-compat with legacy JSON-decoded paths. **Zeroization preserved**: `phrase_secret` + `sync_wallet` drop sequence unchanged; `Secret<String>` zeroizes on handle free as before. **L12 review applied** (critical-tier 1-agent pre-PR per L13 step 10): 1 CRITICAL (C1 `lastError` dead-parameter — wired via `ffi_last_error_message` Dart binding; banner body now shows the actual diagnostic) + 2 IMPORTANT (`FfiSyncStatus.unknown` ABI drift fallback — fails loud via `syncFailed` branch; 5/6 swallow arms lack Rust tests — MAX_MONEY overflow, SPKI fallback, `EsploraClient::new` fail, malformed CStr — backlog) + 2 MEDIUM (`WalletSyncStatus` enum moved to top of file; Retry button `_running` gate) + 1 LOW (3-way `if/else if` → kept as-is). **TDD red-green**: 1 new Rust test `wallet_show_unreachable_esplora_returns_sync_failed` (TCP port 1 → connection refused; asserts `SyncFailed` + `balance: 0` + non-null `first_address`) + 2 existing Rust tests updated for new out-param + 1 new Dart widget test `WalletDetailScreen renders red sync-failed banner + Retry` (`skip:` — pre-existing provider-override infra issue, sibling tests same; Rust FFI test is the canonical GREEN assertion). Cargo fmt + clippy + test clean (4/4 `wallet_show_*` tests pass; full suite 343+ pass). **Migration impact**: `WalletDetail.syncStatus` + `lastError` are additive optional fields — legacy JSON / pre-#263 callers see `FfiSyncStatus.emptyWallet` default + `lastError: null` + unchanged UX. **Out of scope** (deferred to follow-up): additional Rust tests for MAX_MONEY / SPKI / `EsploraClient::new` / malformed-CStr swallow arms; `flutter test` infra fix for `wallet_detail_screen_test.dart` provider overrides. **Pre-flight gotcha (operator-driven)**: rebuild the cdylib via `bash wallet-desktop/tool/build_native.sh` before launching the UI after any Rust ABI change — Dart caches FFI symbols at process start. C1 update is Dart-only — hot-reload picks up without rebuild.

## [v0.2.0] - 2026-08-21

- **`[internal]`** `rust-wallet-app/crates/bitcoin-wallet-core/src/ffi/bdk_extras.rs` + `rust-wallet-app/crates/bitcoin-wallet-core/tests/ffi_smoke.rs` + `rust-wallet-app/crates/bitcoin-wallet-core/src/ffi/mod.rs` + `wallet-desktop/lib/core/ffi/esplora_bindings.dart` + `wallet-desktop/lib/core/wallet_core.dart` (Task 14 #220 Sub-split A, PR #249, SHA `91a236d`) — `wallet_load` + `wallet_load_free` FFI surface. SendScreen needs to call `wallet_send(rt, wallet_handle, esplora_handle, ...)` against an existing wallet on disk; `wallet_from_mnemonic` constructs a NEW wallet from a mnemonic — wrong path for SendScreen. `wallet_load` reads the persisted changeset from `{base_dir}/{wallet_id}.wallet.db` (`.wallet` is the v0.2 schema-version marker; bdk_file_store appends `.db`). L12 CRITICAL #2: phrase wrapped in `Secret<String>` (zeroize-on-drop). L12 MED hostile-caller: length caps on `base_dir` (4096) + `wallet_id` (64). TDD red-green: 2 new `ffi_smoke` regression tests (`wallet_load_free_accepts_null`, `wallet_load_nonexistent_dir_returns_null`) — 7/7 tests pass. Companion Dart binding + facade: `EsploraBindings.walletLoad` + `WalletCore.walletLoad({baseDir, walletId, phrase: SecretBuffer, network})` (L12 CRITICAL #2 SecretBuffer pattern). **Scope NOT included** (deferred to follow-up PR): `WalletSession` + `WalletSessionNotifier` handle storage + lifecycle; `send_screen.dart` FFI migration (Sub-split B).

- **`[internal]`** `wallet-desktop/tool/build_native.sh` (new) + `rust-wallet-app/crates/bitcoin-wallet-core/tests/ffi_smoke.rs` + `.github/workflows/wallet-desktop-ci.yml` (Task 18 #224, PR #248, SHAs `4528049` + `e1185d3`) — native lib build infra. Bash script: detect host arch (linux-x64 / macos-x64 / macos-arm64 / windows-x64; `TARGET_ARCH` override) + `cargo build --release -p bitcoin-wallet-core` + mkdir + cp to `wallet-desktop/native/<arch>/`. Replaces inline `Build bitcoin-wallet-core cdylib` + `Copy cdylib to Flutter native dir` steps in CI. **MEDIUM security finding caught by automated review of `4528049`** (fixed in `e1185d3`): script produced paths that did NOT match the Dart loader's `_HostOs` enum in `wallet-desktop/lib/core/ffi/native_lib.dart`. macOS x64 wrote `native/darwin-x64/` but Dart reads `native/macos-x64/`; macOS arm64 wrote `native/darwin-arm64/` but Dart reads `native/macos-arm64/`; Windows wrote `librust_wallet_core.dll` (with `lib` prefix) but Dart reads `rust_wallet_core.dll` (no prefix, per Windows convention). Linux was correct. Bug never surfaced because CI ran only on `ubuntu-latest`. Fix: rename `darwin-{x64,arm64}` → `macos-{x64,arm64}`; per-platform `cargo_basename` + `dst_filename` table; force-unlink destination before copy; post-copy assertion (file exists + non-empty; exit code 4); cross-check produced path against Dart `_HostOs` enum via grep (exit code 5). Pattern: cross-stack producer/consumer grep cross-check catches future key drift at build time. Also adds `tests/ffi_smoke.rs` (5 integration tests pinning the FFI surface — `ffi_version_*`, `runtime_new_*`, `runtime_drop_*`).

- **`[internal]`** `rust-wallet-app/crates/bitcoin-wallet-core/src/ffi/error.rs` + `wallet-desktop/lib/core/ffi/ffi_exception.dart` (PR #247, Issue #242, SHA `c99fa1f`) — docstring-vs-impl drift fix on `sanitize_for_ffi`. The Rust-side `sanitize_for_ffi` was claimed in docstrings to redact BIP-39 word sequences (12/15/18/21/24 lowercase words) + 64-char hex hashes, but the implementation only replaced NUL bytes with U+FFFD. The "defense-in-depth" L12 CRITICAL #2 closure claim was one layer (Dart `toString` exclusion), not two. Stripped the false claims from both Rust + Dart docstrings; reframed the Dart side as the SOLE defense. 2 regression tests added (`bip39_word_sequence_passes_through_unchanged`, `hex_64char_passes_through_unchanged`) — if a future contributor adds real Rust-side scrubbing, these tests fail and force contract update. Trivial tier per L13 complexity table — L12 pre-PR review skipped.

- **`[internal]`** Task 14 (#220) SendScreen FFI migration — **Sub-split A done** (PR #249, SHA `91a236d`, see entry above); **Sub-split B deferred to v0.2.1**. Sub-split A delivered the FFI surface (`wallet_load` + Dart binding + facade) that SendScreen will consume. Sub-split B — `WalletSession` + `WalletSessionNotifier` handle storage + lifecycle, plus `send_screen.dart` FFI migration — requires UX decisions (eager vs lazy handle creation; where to surface "password required to decrypt" prompts) deferred to a focused follow-up PR. L29 live testnet smoke for `wallet_load` also deferred. (Initial Task 14 pickup 2026-08-20 reverted per L13 Q9 off-rails recovery when scope discovery showed the plan's surface — `walletCore.send({mnemonic, recipient, amountSat, feeRateSatPerVb, esploraConfig})` — under-scoped the work; the handle-based FFI surface landed in Tasks 3-5 + Sub-split A is the architecturally correct path per L33.4 — mnemonic never crosses FFI as raw bytes.)

- **`[internal]`** `wallet-desktop/lib/features/wallet_detail/wallet_detail_screen.dart` + `wallet-desktop/lib/providers/wallet_providers.dart` + `wallet-desktop/lib/core/btc/models/wallet_detail.dart` + `wallet-desktop/lib/widgets/balance_card.dart` + `wallet-desktop/lib/core/wallet_core_api.dart` + `wallet-desktop/lib/core/wallet_core.dart` + `wallet-desktop/lib/core/ffi/wallet_ops_bindings.dart` + `wallet-desktop/lib/providers/btc_providers.dart` + `wallet-desktop/pubspec.yaml` + `.github/workflows/wallet-desktop-ci.yml` (Task 13/20, PR #246, Issue #219, SHA `877a5e8`) — `WalletDetailScreen` FFI migration + **btc CLI removal**. Critical-tier (mnemonic + password + irreversible broadcast surface). Adds `wallet_show` FFI export in `bitcoin-wallet-core/src/ffi/wallet_ops.rs` taking `(network, base_dir, wallet_id, password, password_len, out_id[37], out_network, out_address_type, out_first_address, out_balance_sat)` + `wallet_show_first_address_free` CString free. N2 oracle mitigation collapse: file-not-found / wrong-password / wrong-AAD / corrupt-blob / non-mnemonic-plaintext all surface as `FfiError::WalletStore` (single indistinguishable copy — UI renders via `userMessageForFfiException`). L12 CRITICAL 3-agent cascade applied (compass:security-auditor + pr-review-toolkit:type-design-analyzer + ecc:flutter-reviewer — N2 collapse was the security finding). **Plan deviations**: `Balance` collapsed from 4-tuple to single `confirmedSat` field (Rust `wallet_show` returns `balance_sat: u64`; the legacy confirmed/trustedPending/untrustedPending/immature breakdown is deferred to v0.2.1 once the Esplora sync is wired); `WalletDetail.utxos` field dropped (Rust `wallet_show` v0.2.0 read-only show, no sync — `wallet_utxos` FFI deferred to v0.2.1); `firstAddress` is empty in v0.2.0 (Rust `peek_addresses` requires bdk sync). `BalanceCard` shows hint when balance == 0. v0.2.0 L12 deferred items: typed CString wrapper, `needsSync` flag, Balance 4-tuple restore, `_FakeWalletCore` extract, `drop(pw_secret)`, WalletDetail equality. **btc CLI removal**: `lib/core/binary/btc_extractor.dart` + `assets/btc/` (5 binaries) + `.github/workflows/btc-bundle.yml` + `test/integration/wallet_lifecycle_test.dart` + `test/integration/fixtures/{fake_btc,with_secret_env}.sh` + 8 btc/* unit test files + `scripts/build_fake_btc.sh` deleted (moved to `/tmp/wallet-desktop-btc-removed-2026-08-20/` for archival); `pubspec.yaml` `assets:` block removed; `btcInvokerProvider` stubbed (throws `StateError` on access); `.github/workflows/wallet-desktop-ci.yml` `Build fake_btc` step dropped (CI fix in SHA `f04d695` — the deleted `test/integration/fixtures/fake_btc.sh` was a real regression caught on first Task 13 CI run). 4 test files updated (`test/unit/wallet_detail_screen_test.dart`, `transactions_screen_test.dart`, `send_screen_test.dart`, `widgets_test.dart`, `dtos_test.dart`, `wallets_list_provider_test.dart`, `wallet_list_screen_test.dart`); `_FakeWalletCore` carries `showWallet` throws `UnimplementedError`. TDD red-green: 3 new Rust tests (`wallet_show_after_create_returns_matching_metadata`, `wallet_show_wrong_password_returns_wallet_store`, `wallet_show_first_address_free_null_is_noop`); Flutter verify gate clean. **Unblocks Tasks 14-16**: SendScreen / TransactionsScreen / SettingsScreen + FeeEstimates FFI migrations.

- **`[internal]`** `wallet-desktop/lib/features/wallet_import/wallet_import_screen.dart` (Task 12/20, PR #245, Issue #218, SHA `ef4cb12`) — `WalletImportScreen` FFI migration. Replaces `BtcInvoker.invoke<WalletInfo>(BtcCommand.walletImport(...), parse: ...)` with `walletCore.importWallet({network, phrase: SecretBuffer, password: SecretBuffer, baseDir})` → `WalletImported`. Mnemonic paste via `SecretBuffer.fromUtf8` (Task 8 RAII — zeroize on dispose). L12 CRITICAL #2 closed at FFI boundary (mnemonic never a Dart `String` past the paste-field handler). `withPasswordFile` temp-file plumbing deleted; `BtcLogFilter.redact` still applies on the catch path. v0.1 mnemonic-in-argv gap (`BtcCommand.walletImport --mnemonic <words>`) closed. Verify gate clean: `dart analyze --fatal-warnings --fatal-infos` no issues; widget test passes.

- **`[internal]`** `wallet-desktop/lib/features/wallet_create/wallet_create_screen.dart` + `wallet-desktop/lib/features/wallet_create/mnemonic_display_dialog.dart` (Task 11/20, PR #244, Issue #217, SHA `4d1ba30`) — `WalletCreateScreen` + `MnemonicDisplayDialog` FFI migration. **Closes F47 zeroization gap** (mnemonic crosses FFI boundary as `MnemonicHandle` Box-allocated newtype with `Drop` impl — zeroize + free on `phrase_view_free`; Dart side reads via `phrase_view_copy` zero-copy `*const c_char` then disposes `MnemonicView`). Replaces `BtcInvoker.invoke<WalletCreated>(BtcCommand.walletCreate(...))` with `walletCore.createWallet({words, network, addressType, password: SecretBuffer, baseDir})` → `WalletCreated` with `MnemonicView` (plaintext phrase NEVER a Dart `String` field on the DTO). Dialog `pop` blocked until Reveal + Ack both flipped (`PopScope(canPop: _acked)`); mnemonic masked by default; `_everRevealed` monotonic guard. L12 3-agent cascade applied. TDD red-green: 3 widget tests + Rust `wallet_create`/`wallet_import` happy/sad-path tests. Verify gate clean.

- **`[internal]`** `wallet-desktop/lib/providers/wallet_providers.dart` + `wallet-desktop/lib/core/wallet_core_api.dart` + `wallet-desktop/lib/core/wallet_core.dart` + `wallet-desktop/lib/core/ffi/wallet_ops_bindings.dart` + `wallet-desktop/test/unit/wallets_list_provider_test.dart` + `wallet-desktop/test/unit/wallet_list_screen_test.dart` (Task 10/20, PR #243, Issue #216, SHA `b71dac0`) — `WalletsListNotifier` FFI migration. async `walletsListProvider(network)` reads from `walletCoreProvider.listWallets(...)` instead of `btcInvokerProvider`. First real `FfiException` consumer: `FfiException(io)` and `FfiException(walletStore)` surface via `AsyncError` — `WalletListScreen` matches on `kind` to render user-facing messages. L34.1 guard: empty list returns `AsyncData([])` (NOT `AsyncError` — Rust may legitimately return 0-length). Mockability seam: `_FakeWalletCore implements WalletCoreApi` (4 tests — fresh-install empty + populated + `FfiException(io)` + `FfiException(walletStore)` + refresh). `walletCoreApi` is the abstract interface; `WalletCore` is the FFI impl. TDD red-green. Verify gate clean.

- **`[internal]`** `wallet-desktop/lib/core/ffi/ffi_exception.dart` + `wallet-desktop/lib/core/wallet_core.dart` (Task 9/20, PR pending, Issue #215) — typed `FfiException` hierarchy translating the 21 `FfiError` ABI codes from `rust-wallet-app/crates/bitcoin-wallet-core/src/ffi/error.rs` (+ `panic` + `unknown` sentinels = 23 total kinds) into Dart exception classes. Closes **L12 CRITICAL #1** (deferred from Task 7 PR #239 review): the `WalletCore._ffiError` helper no longer throws a generic `Exception` placeholder — UI screens in Tasks 10-16 will match `on FfiException catch (e) when (e.kind == FfiErrorKind.insufficientFunds)` to render user-facing messages instead of parsing string dumps. New type: `enum FfiErrorKind { invalidMnemonic, invalidDerivationPath, addressDerivation, scriptBuild, network, esplora, electrum, insufficientFunds, txBuild, sign, psbt, storage, notInitialized, encryption, mnemonicCipher, walletStore, bitcoin, bdk, io, bip137, spkiPin, panic, unknown }` (23 cases, frozen ABI — renumbering forbidden). `final class FfiException implements Exception` with named ctor `_` (private) + factory `FfiException.fromCode({required int code, required String op, String? message, Object? cause})`. Fields: `kind`, `code`, `op`, `message?`, `cause?`. **L12 CRITICAL #2 redaction contract (defense-in-depth)**: `toString` returns `'FfiException(op: $op, kind: ${kind.name}, code: $code)'` and NEVER includes the `message` field — the Rust-side `sanitize_for_ffi` (Task 2) already scrubs 12/15/18/21/24-word sequences and 64-char hex on the FFI payload; the Dart-side `toString` contract is independent defense against accidental crash-reporter / log-file leakage if the Rust sanitizer ever regresses. `WalletCreated.toString` and `BtcLogFilter.redact` already follow the same pattern. `unknown` fallback covers future codes added upstream while `Error` remains `#[non_exhaustive]`. **TDD red-green**: 32 new tests (23 explicit code→kind mapping tests + 2 sealed-hierarchy invariants + 3 toString redaction tests including mnemonic-byte + password-byte + no-message cases + 2 cause-chaining tests + 2 fallback tests for unknown/Ok codes); all `flutter test` pass — `202 + 6 skip` (was `170 + 6`, +32 new, 0 regression). Existing `wallet_core_test.dart` assertion tightened: was `throwsA(isA<Exception>())` → now `throwsA(isA<FfiException>().having((e) => e.kind, 'kind', equals(FfiErrorKind.invalidMnemonic)))`. Verify gate clean: `dart format --set-exit-if-changed` 4 files reformatted (ffi_exception.dart + ffi_exception_test.dart + wallet_core.dart + wallet_core_test.dart); `dart analyze --fatal-warnings --fatal-infos` no issues. **Unblocks Tasks 10-16**: screen-by-screen migration of `BtcInvoker` consumers can now use typed exception matching.

- **`[internal]`** `wallet-desktop/lib/core/ffi/{secret_buffer,mnemonic_view}.dart` + `wallet-desktop/lib/core/wallet_core.dart` + `wallet-desktop/lib/providers/wallet_core_provider.dart` (Task 8/20, PR #240, Issue #214) — typed `WalletCore` facade over the Task 7 binding surface. Closes **L12 CRITICAL #2** (mnemonic + password never appear as `String` in the Dart heap; Rust `Secret<T>` is mirrored by Dart `SecretBuffer` RAII newtype that zeroizes + frees the heap allocation on `dispose()`; `MnemonicView` wraps `Pointer<Void>` so the plaintext phrase is NEVER a `String` field on the returned `WalletCreated` DTO). Three new files: `secret_buffer.dart` — `SecretBuffer.allocate(List<int>)` + `SecretBuffer.fromUtf8(String)` factories; `ptr` + `length` getters; `dispose()` idempotent, zeros heap via `fillRange(0, allocLen, 0)` then `calloc.free`; no `toString`/`==`/`hashCode` overrides (defensive masking via inherited `Object` defaults). `mnemonic_view.dart` — `MnemonicView(Pointer<Void>)` constructor; `String read()` lazy phrase copy via `phraseViewCopy`; `dispose()` calls `phraseViewFree` exactly once, nulls `_cached` String + `_handle`; no `toString` override. Replaces Task 1 spike `wallet_core.dart` with full typed surface: `ffiVersion()`, `listWallets({network: FfiNetwork, baseDir})`, `deleteWallet(...)`, `createWallet({words, network, addressType, password: SecretBuffer, baseDir})` → returns `WalletCreated` with `MnemonicView`, `importWallet(...)` → returns `WalletImported`, async surface (`esploraClientNew`, `walletFromMnemonic`, `walletSync`, `walletBalance`, `walletSend`, `walletTxids`, `walletPeekAddresses`) wrapping `EsploraBindings` + `RuntimeBindings`. Co-located `WalletCreated` + `WalletImported` DTOs (shadows legacy `lib/core/btc/models/wallet_created.dart` String-mnemonic DTO — distinct import paths). Process-lifetime `walletCoreProvider = Provider<WalletCore>` for Riverpod consumers (NOT autoDispose — holds tokio runtime handle). **L12 CRITICAL #2 deferred to Task 9**: `FfiError` codes are translated to generic `Exception` placeholder (`Exception('WalletCore FFI error: $op returned $code')`); Task 9 swaps to `FfiException` with typed kinds. **Plan deviations**: `walletFromMnemonic` internally allocates NUL-terminated `Pointer<Utf8>` from `SecretBuffer` bytes via `_toCString` helper (Rust expects `*const c_char` per Task 5 ABI); `firstAddress` is NOT a field on `WalletCreated`/`WalletImported` (Rust `wallet_create`/`wallet_import` do not write first_address — UI derives it via `wallet_peek_addresses` in Tasks 10-16); `phrase.ptr` cast for `walletFromMnemonic` uses a fresh NUL-terminated copy rather than reusing the SecretBuffer bytes. **TDD red-green**: 17 new tests (8 SecretBuffer + 4 MnemonicView + 5 WalletCore); all `flutter test` pass — `170 + 6 skip` (was `153 + 6`). Verify gate clean: `dart format --set-exit-if-changed` 7 files reformatted; `dart analyze --fatal-warnings --fatal-infos` no issues. **Unblocks Tasks 9-16**: `FfiException` translator + screen-by-screen migration of `BtcInvoker` consumers. v0.2.0 backlog: real `NativeFinalizer` registration for SecretBuffer GC-time zeroize (currently relies on deterministic `dispose()`), `WalletId(Uuid)` typed wallet-id (currently String).

- **`[internal]`** `wallet-desktop/lib/core/ffi/{wallet_ops_bindings,esplora_bindings,runtime_bindings,ffi_enums}.dart` (Task 7/20, PR pending, Issue #213) — typed FFI wrappers for the 27 C ABI exports from Tasks 3+4+5 (ffi_version + version_free, wallet_create + phrase_view_copy/free + wallet_list + list_array_free + wallet_delete + wallet_import, runtime_new + runtime_drop, esplora_client_new + free + fee_estimate + estimate_free + broadcast_tx + broadcast_tx_free, wallet_from_mnemonic + wallet_free + wallet_sync + wallet_balance + wallet_send + send_free + wallet_txids + txids_array_free + wallet_peek_addresses + peek_addresses_array_free). Typed enum constants in `ffi_enums.dart` (`FfiNetwork` / `FfiAddressType` / `FfiKeychainKind`) encode the `u8` byte values for the small-scalar parameters (L12 review H3 — Dart `int` is 64-bit, raw `1000` silently truncates to `232` via `% 256` and surfaces as `FfiError::Unknown` downstream). **L12 review applied** (parallel: `ecc:type-design-analyzer` + `ecc:code-reviewer` — 2 CRITICAL + 4 HIGH + 5 MED + 4 LOW findings; HIGH/MED round-1 applied): `outId` typed as `Pointer<Uint8>` (H1 — Rust writes 36 bytes, no NUL; was `Pointer<Utf8>` mismatch); scalar `int` parameters documented as `FfiNetwork.<v>.code` (H3); `calloc.allocate` → `calloc<T>()` docstring fix (code-reviewer MED); 2 sync smoke tests added for `esploraClientNew` + `walletFromMnemonic` handles (no network, mirrors Rust unit tests — code-reviewer MED). **L12 CRITICAL #2 deferred to Task 8 facade**: `Pointer<Utf8>` for mnemonic + password bytes enables `.toDartString()` leak vector — needs a `SecretBuffer` RAII newtype (L12 C1 + C2) + `PhraseView` typed handle (H4) + zeroing affordance (H2); scope of this task is "thin typed wrappers, no manual `Pointer<NativeFunction>` plumbing" so the newtype lives at the Task 8 wallet_core facade layer. Secret-leak sweep clean (`rg -n '(password|mnemonic|secret)\s*[:=]\s*"[^"]+"' lib/` returns 0). **Native lib drift surfaced**: cargo produces `libbitcoin_wallet_core.so` (Rust crate naming); NativeLib expects `librust_wallet_core.so` (Task 6 convention). cp + rename applied as local work-around; backlog issue #238 filed for Task 18 build-native infra. **Plan deviations**: `walletFromMnemonic.phrase` kept as `Pointer<Utf8>` (matching Rust `*const c_char` NUL-terminated contract) — type-design review suggested `Pointer<Uint8>` + length but that would break the Rust-side `CStr::from_ptr` path. Verify gate clean: `dart format --set-exit-if-changed` 0 changes; `dart analyze --fatal-warnings --fatal-infos` no issues; `flutter test` 41 pass + 2 skip (16 symbol-resolution + 2 sync smoke for Esplora + 9 symbol-resolution + 1 smoke for wallet ops + 2 symbol-resolution + 1 smoke for runtime). **Unblocks Tasks 8-9**: `WalletCore` facade + `FfiException` translator.

- **`[internal]`** `bitcoin-wallet-core/src/ffi/bdk_extras.rs` (Task 5/20, PR #232, Issue #211) — 9 FFI exports wrapping `EsploraClient` (new + fee_estimate + broadcast_tx) and `Wallet` (from_mnemonic + sync + balance + send + txids + peek_addresses) async methods via the Task 3 tokio runtime handle. **L12 CRITICAL 3-agent review applied** (compass:code-reviewer + ecc:type-design-analyzer + ecc:security-auditor — 12 findings, 7 CRITICAL/HIGH applied). Threat-model closure: **L12 CRITICAL #2** — `wallet_from_mnemonic` phrase wrapped in `Secret<String>` (zeroize-on-drop) before `Mnemonic::from_phrase` (initial impl was a plain `String`; caught by 2/3 L12 agents as a doc/code discrepancy — **L41 harvested**); recipient address uses `Address::require_network(wallet.network())` rejecting a mainnet address typed into a testnet wallet's send form (was `assume_checked()` skip — F25 / U6); **F20 SPKI enforcement** — `esplora_client_new` rejects null SPKI pin on non-localhost hosts (was default `TlsPolicy::SystemRoots` — rogue-CA MITM vector); all `Err(_) => FfiError::Unknown` blanket mappings replaced with `FfiError::from(e) + set_last_error(scrubbed msg)` (the 21-variant `FfiError` enum is now actually exercised at the FFI boundary). Other hardening: `EsploraHandle` / `WalletHandle` newtypes actually used (`Box::into_raw` round-trip — were dead `#[allow(dead_code)]` shells); array free **L40 pattern** (count embedded at `arr-8`, caller count ignored — was trusted, UB on tampering); `fee_rate_sat_per_vb: u64` directly (was `*const c_char -> f64` — NaN/Inf landmine closed); `address_type` byte mapping harmonized with Task 4 (0=NativeSegwit, 1=NestedSegwit, 2=Taproot); every `unsafe` block has inline `// SAFETY:` comment; new `ffi_catch_unwind_ptr<T>` generic helper for pointer-returning FFI exports (replaces copy-paste `Cell<*const c_char>`); `build_cstring_array` / `free_cstring_array` shared helpers for txids + peek_addresses (was duplicated 13 lines × 2). **Plan deviations** (vs. `2026-08-19-flutter-ffi-bitcoin-wallet-core.md`): `wallet_from_mnemonic` keeps `*const c_char` (vs. plan's `phrase: string`) for cross-task ABI consistency with Task 4's password parameter; `wallet_handle` survives across `sync`/`balance`/`send` calls (plan suggested one-shot send); `address_type` byte mapping follows Task 4's `parse_address_type` (not the plan's simplified 0/1 mapping). 13 unit tests (10 new in `bdk_extras.rs` + 3 from L40 round-trip test for `build_cstring_array`). Verify gate clean: `cargo test --lib` 331/0/8; `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` clean. v0.2.0 backlog: Tasks 6-9 (Dart FFI bindings + facade) + Task 16 (Settings screen migration) + Task 18 (native lib build infra `librust_wallet_core.so`).
- **`[internal]`** `wallet-desktop/lib/core/ffi/native_lib.dart` (Task 6/20, PR pending, Issue #212) — Dart FFI `DynamicLibrary` loader for `bitcoin-wallet-core`. Platform switch (Linux `librust_wallet_core.so` / macOS arm64+x64 `librust_wallet_core.dylib` / Windows `rust_wallet_core.dll`) resolved against `native/<arch>/` subdir. macOS arm64 vs x64 detected at runtime via `Platform.resolvedExecutable.contains('arm64')` (mirrors `btc_extractor.dart:hostTarget()` pattern; closes the Intel-mac `FileSystemException` footgun caught by L12 code-reviewer H1+H2). TDD red-green cycle: 10 tests pass + 2 platform-gated skip on non-host. **L12 review applied** (parallel: `ecc:type-design-analyzer` + `ecc:code-reviewer` — 0 CRITICAL, 6 HIGH + 6 MED + 5 LOW + 6 INFO findings; 8 H/M applied in single round-trip): private `_HostOs` enum with arch variants (`linux` / `macosArm64` / `macosX64` / `windows`) collapses parallel switches in `defaultBasePath` + `libNameForPlatform` (H1 — single source of truth for `subdir`/`libName`); `_HostOs.detect()` throws `UnsupportedError` directly on non-supported hosts (H2 — removes indirect fall-through via `Platform.operatingSystem`); empty + whitespace-only `basePath` rejected with `ArgumentError` (L1 + H4); `skip:` argument on platform-gated tests so wrong-host runs report as skip not silent pass (M3 + L12 M1). `p.join` from `package:path` for path assembly (L1 — matches `lib/core/paths.dart` + `btc_extractor.dart` convention). new `does-not-exist` failure-path test (M2 — catches load-failure swallow regressions). **Plan deviations**: kept `libNameForPlatform(String)` instead of `(_HostOs)` enum (H1's M1 sub-finding) so callers can pass `'linux'`/`'macos'`/`'windows'` string literals; the arm64/x64 split lives in `_HostOs.detect()` (runtime) not the public string API. Verify gate clean: `dart format --set-exit-if-changed` 0 changes; `dart analyze --fatal-warnings --fatal-infos` no issues; `flutter test` 10 pass + 2 skip. **Unblocks Tasks 7-9**: typed FFI wrappers + WalletCore facade + FfiException. v0.2.0 backlog: typed `HostOs` enum exposure for callers (if Tasks 7-9 want it), Task 18 build-native infra.

## [0.1.0] - 2026-08-15

### Added

- **`[internal]`** `wallet-desktop/` Flutter scaffold — `lib/core/paths.dart` (`appDataDir()` + `subdirFor(name)` via `path_provider` + `path.join`; recursive mkdir idempotent across runs) + `lib/core/theme.dart` (`buildLightTheme()` + `buildDarkTheme()` Material 3 themes derived from `bitcoinOrange = Color(0xFFF7931A)` seed). Task 3/26 of the wallet-desktop plan (PR #175). Foundation for all subsequent tasks; no user-facing CLI/screen changes yet. 3 unit tests pin path resolution + idempotency; verify gate `dart format` + `dart analyze --fatal-warnings --fatal-infos` + `flutter test` clean.
- **`[internal]`** `wallet-desktop/core/binary/btc_extractor.dart` (Task 4/26, PR #179) — extracts bundled `btc` binary from `assets/btc/<hostTarget>/btc[.exe]` to a temp dir. `hostTarget()` returns `linux`/`macos`/`windows` per `Platform.operatingSystem`. `extractBtc()` writes with `FileMode.writeOnly` (no truncate-after-open race) and validates the asset exists before write. `sealed class BtcException` + `UnsupportedPlatformException` for typed error mapping. 4 unit tests + `ThrowingPathProvider` test helper. v0.2 backlog: cache-hit test (currently only cold-extract covered).
- **`[internal]`** `wallet-desktop/core/secret/temp_secret_file.dart` (Task 5/26, PR #181) — `TempSecretFile` for mode-0600 secret-passing (mnemonic, password) to `btc` subprocess. write → `chmod 600` (not 0o600 literal — string `"600"`; GNU/BSD chmod reject Dart `0o` prefix) → rename-to-temp pattern closes write→chmod race window. `FileMode.writeOnly` (no truncate-after-rename). `SecretException` sealed base parallels Task 4 `BtcException`. Threat-model: F5 (no world-readable temp file), F47 (zeroize before delete), F19 (no partial ciphertext). chmod-verify test deferred to Task 25 (CI needs chmod permission).
- **`[internal]`** `wallet-desktop/core/security/password_supply.dart` (Task 6/26, PR #182) — `withPasswordFile(String password, Future<T> Function(String) body)` thin delegate over `withTempSecretFile` (Task 5). 1-line bridge, no new logic; centralized so future secret types (BIP-39 passphrase, encryption key) reuse the same `withTempSecretFile` plumbing.
- **`[internal]`** `wallet-desktop/core/security/btc_log_filter.dart` (Task 7/26, PR #183) — `BtcLogFilter` scrubs mnemonic + password from `package:logging` output before STDOUT/STDERR. Closes L12 CRITICAL #2. 5 bypasses defended: `\s+` separator (CRITICAL — tab/newline-separated mnemonic previously emitted `<redacted>` marker while secret stayed in log, worse than no filter), case-insensitive matching, `=` form, `LogFilter` abstract interface (since `package:logging` 1.3.0 has no `LogFilter` export), `const BtcLogFilter()` constructor. 16 unit tests + 1 LOW doc-typo fixup.
- **`[internal]`** `wallet-desktop/core/btc/btc_command.dart` + `btc_error.dart` (Task 8/26, PR #184) — `BtcCommand` enum (29 variants, one per `btc` CLI subcommand) + sealed `BtcError` classifier. CRITICAL fixes: `BtcError.toString()` no longer leaks `stderr` content (defends F25 — descriptor echo vector); CLI flag inconsistency (`--esplora-spki-pin` vs `--pin-spki`) unified to `--pin-spki`. 3 plan drifts: extension static → class static (Dart 3 dispatch), collection-if local-capture pattern (Dart 3 inference), BtcError pattern-match order more-specific-first. 2 fixup commits.
- **`[internal]`** `wallet-desktop/core/dto/*.dart` (Task 9/26, PR #185) — 7 DTOs (WalletInfo, WalletDetail, WalletCreated, WalletRecord, Fee, Send, Config) + `DtoParseException` (typed exception with field path, replaces uncaught TypeError). HIGH findings: `TxDirection.unknown` variant (no silent 'outgoing' default); `WalletCreated.toString()` masks mnemonic; `List.unmodifiable` for collections; is-List guard before silent fallback (closes Dart inference issue + silent-fall-through path). 3 contract tests added (mnemonic mask, TxDirection.unknown, DtoParseException fire).
- **`[internal]`** `wallet-desktop/core/btc/btc_invoker.dart` (Task 10/26, PR #186) — spawns `btc` subprocess + parses `--json` stdout. CRITICAL fix (caught by code-reviewer post-PR; all 3 pre-PR sub-agents missed): `Process.start` `includeParentEnvironment` defaults to `true` so the L7 env-strip was a no-op — parent shell's `BTC_WALLET_MNEMONIC` was re-inherited after the strip. Fixup commit: `includeParentEnvironment: false`; `.ignore()` on pipe futures before timeout; SIGTERM → SIGKILL after 2s grace; `$e` interpolation replaced with constant. Threat-model: F25 (no mnemonic in argv or env), F7 (defensive subprocess termination), F25 (no stderr leak in BtcError).
- **`[internal]`** `wallet-desktop/providers/btc_providers.dart` (Task 11/26, PR #187) — `appPathsProvider` (FutureProvider — resolves `appDataDir` + 3 subdirs `btc/` + `tmp/` + `wallet_data/`) + `btcInvokerProvider` (FutureProvider — composes paths + `extractBtc` + `BtcInvoker` with `dataDirOverride`). Single `ref.watch` per provider (collapsed redundant watch + variable capture). Explicit `Ref` type annotation. v0.2 backlog: `extractBtc()` testability seam, `dataDirOverride` contract test, `AppPaths` directory-hierarchy assert, provider disposal test.
- **`[internal]`** `wallet-desktop/providers/esplora_config_provider.dart` (Task 12/26, PR #188) — `EsploraConfig` (network + url + F20 SPKI pin) const + final + factory defaults + JSON round-trip + `copyWith`. `EsploraConfigNotifier extends AsyncNotifier<EsploraConfig>` with `build()` reading JSON file or `defaults('testnet')` + `save()` writing through. `esploraConfigFilePathProvider` is override-required (no surprise global state). v0.2 backlog: encrypted-at-rest for SPKI pin (F20 cleartext JSON), atomic file write (write-to-temp + rename). 3 unit tests pin defaults + JSON round-trip + disk persistence.
- **`[internal]`** `wallet-desktop/providers/wallet_providers.dart` (Task 13/26, PR #189) — `walletsListProvider = AsyncNotifierProvider.autoDispose.family<WalletsListNotifier, List<WalletInfo>, String>`. Composes `btcInvokerProvider` + `BtcCommand.walletList` + `WalletInfo.fromJson` parse. `refresh()` delegates to `ref.invalidateSelf()` + `await future` (Riverpod dep-graph preserved; manual `build(arg)` re-invocation would race concurrent invalidations + double-subscribe btcInvokerProvider — flagged HIGH by flutter-reviewer + type-design). Null/empty stdout fallback: BtcInvoker returns `null` on empty stdout / `String` on non-JSON; fresh install now reports `data: []` instead of `BtcError(kind: other)`. 3 unit tests exercise parse + refresh + empty/null paths via `_FakeBtcInvoker` test double. v0.2 backlog: `String` → `Network` enum for family arg, oversized-payload DoS cap, EsploraConfig.save() atomic write, EsploraConfig.save() cb → next param rename.
- **`[internal]`** `wallet-desktop/providers/wallet_providers.dart` (Task 14/26, PR #190) — `walletSessionProvider = NotifierProvider.family<WalletSessionNotifier, WalletSession?, String>` (non-autoDispose). In-memory unlocked session holding `walletId` + `OpaqueMnemonic` + `WalletDetail?`. `unlock(mnemonic, detail)` derives walletId from family arg (closes cross-key footgun), disposes prior mnemonic before overwrite (closes heap leak — flagged CRITICAL by type-design + HIGH by flutter-reviewer). `ref.onDispose` cleanup in `build()` fires on ProviderContainer teardown (safety net for app exit when explicit lock() never called). `OpaqueMnemonic` renamed from `ZeroizingString` to honestly signal "we hold a reference, not actual zeroization" (Dart strings are immutable + interned — `_value=''` only clears the field, not the heap; real zeroization requires FFI Uint8List + Finalizable, v0.2). `lock()` disposes mnemonic + clears state. 6 unit tests including re-unlock-disposes-prior regression guard. v0.2 backlog: real FFI zeroize, AppLifecycleState auto-lock, copyWith sentinel, String→WalletId, test mnemonic redaction.
- **`[internal]`** `wallet-desktop/lib/widgets/*.dart` (Task 15/26, PR #191) — 7 reusable widgets for Tasks 17-21: `AddressChip` (tap-to-copy Bitcoin address + snackbar), `BalanceCard` (confirmed + pending breakdown), `NetworkPicker` (SegmentedButton over 5 supported networks, default testnet), `PasswordField` (TextField with obscure + eye toggle), `MnemonicPasteField` (word-count validation + ack checkbox + BIP-39 count assert: 12/15/18/21/24), `StatusBadge` (maps BtcErrorKind to icon + theme-driven color via `Theme.colorScheme.{error,tertiary,outline}`), `ProcessProgressOverlay` (modal scrim via `Theme.colorScheme.scrim/onInverseSurface`). 7 widget tests. Threat-model: F25 (controllers never in print/debugPrint/log paths), F47 caveat documented at widget level (Dart strings are immutable + interned; heap lingers until GC; v0.2 routes through FFI Uint8List + Finalizable). v0.2 backlog: String→Opaque wrappers for onChanged callbacks, validity read-back, controller parameterization, BitcoinUnit enum, tooltips, i18n, perf hoists.
- **`[internal]`** `wallet-desktop/lib/routing/app_router.dart` + `wallet-desktop/lib/features/home_shell.dart` + `wallet-desktop/lib/main.dart` (Task 16/26, PR #192) — Navigation skeleton for Tasks 17-23. `GoRouter` with `ShellRoute` wrapping `HomeShell` + 7 `GoRoute`s (`/`, `/wallets/:network`, `/wallets/:network/new`, `/wallets/:network/import`, `/wallets/:network/:walletId`, `/wallets/:network/:walletId/send`, `/wallets/:network/:walletId/transactions`, `/settings`). `HomeShell` = `NavigationRail` (Wallets + Settings) + content; selectedIndex derived from `GoRouterState.uri.path` (Settings correctly highlights when active); preserves active network when tapping Wallets so mainnet users aren't silently downgraded to testnet. `main.dart` wires `ProviderScope` + `MaterialApp.router` with light + dark theme + system theme mode; GoRouter hoisted to `BtcWalletApp._router` field (per-build would lose history + re-fire redirects). 6 placeholder feature screens (Tasks 17-23 wire real impls). v0.2 backlog: Network enum + redirect allowlist, Settings-driven initial location, errorBuilder, StatefulShellRoute.indexedStack, deep-link URL scheme, walletId redaction in placeholder screens.
- **`[internal]`** `wallet-desktop/lib/features/wallet_list/wallet_list_screen.dart` + `wallet-desktop/lib/routing/wallet_routes.dart` + `wallet-desktop/lib/core/format/wallet_id.dart` + `wallet-desktop/lib/core/btc/btc_error_messages.dart` (Task 17/26, PR #193) — First real screen consumer; reads the `walletsListProvider(network)` autoDispose family (Task 13) and renders Create/Import header buttons + per-wallet `ListTile` rows. `WalletRoutes` constants class is the single source of truth for the wallet-area path templates (compiled-in paths replace the prior `'/wallets/$network/new'` literals scattered across screens); `isValidWalletIdSegment` enforces `[A-Za-z0-9_-]{1,64}` against path-injection (security-auditor MEDIUM, closed in-screen until router-level `redirect:` lands in v0.2). Shared helpers extracted so upcoming Tasks 18/20/21/22 reuse without copy-paste: `formatWalletId(String)` (lib/core/format/wallet_id.dart) applies `first4…last4` truncation for ids > 12 chars; `userMessageForBtcError(BtcError)` (lib/core/btc/btc_error_messages.dart) maps the 7 `BtcErrorKind` variants to user-facing copy. 7 unit tests cover empty state, populated list, id truncation, friendly error + Retry button, navigation-tap callbacks (Create/Import/row-tap), allowlist enforcement, and `WalletRoutes` builder sanity. L12 fixups applied: `TextOverflow.ellipsis` + `maxLines: 1` + `Theme.of(context).textTheme.bodyMedium.copyWith(fontFamily: 'monospace')` on the wallet-id title (flutter-reviewer HIGH theme-bleed), `RefreshIndicator` wrapping the populated list (flutter-reviewer HIGH — promised by `walletsListProvider.refresh()` doc, finally reachable from UI), BtcError-kind-mapped error messages + Retry button + `dart:developer.log` for ops triage + `BtcLogFilter.redact` fallback for non-BtcError throws (security-auditor MEDIUM + flutter-reviewer HIGH error-leak) + `dart:developer.log` exception piped through `BtcLogFilter.redact` (defence-in-depth per post-PR sec-auditor), `ValueKey(w.id)` for ListView element reuse, hoisted null-checks, validator-once 3-branch `onTap` refactor (type-design MEDIUM). v0.2 backlog: Network/AddressType enums (cross-cutting: 13/15/16/17), WalletId value type, WalletListNavigator interface, router-level `redirect:` for `:walletId` (currently screen-side allowlist covers path-injection only — `'new'`/`'import'` route-segment collisions are a UX footgun, not a security boundary), ARB/i18n keys, design-token `AppSpacing.*`.
- **`[internal]`** `wallet-desktop/lib/features/wallet_create/wallet_create_screen.dart` + `wallet-desktop/lib/features/wallet_create/mnemonic_display_dialog.dart` (Task 18/26, PR #194) — Critical-tier screen + dialog (secrets surface: mnemonic + password). Form renders Words (12/24) + Address Type + PasswordField dropdowns; on submit, `withPasswordFile` (Task 5/6) routes the password via a mode-0600 temp file → `BtcCommand.walletCreate` → `WalletCreated.fromJson` → `MnemonicDisplayDialog` once (barrierDismissible: false + `PopScope(canPop: _acked)` closing the tap-outside / Esc / Android-back paths). Dialog masks the mnemonic by default (`'•' * mnemonic.length` rendered as plain `Text` so semantics + clipboard can never reach the cleartext) — Reveal + Ack checkboxes must both flip before Done is enabled; `_everRevealed` flag enforces the ordered Reveal→Ack transition (closing the "user acks without reading" loss path). Threat-model defence-in-depth: password routed via mode-0600 + never enters argv + screen-side `_password = ''` clear before dialog; mnemonic never logged; `dart:developer.log` exception piped through `BtcLogFilter.redact` per Task 17 lesson; non-BtcError catch falls back to `userMessageForBtcError` + `StatusBadge` (Task 15); `ref.invalidate(walletsListProvider)` post-success (NOT `unawaited(notifier.refresh())`, which produced uncaught zone errors on CLI failure + autoDispose race per L12 type-design post-PR HIGH). Post-PR fixups: `BlockSemantics` → `ExcludeSemantics` (the wrong widget for descendants per L12 type-design post-PR HIGH #1 — blocks siblings, not the subtree); `userMessageForBtcError`'s `BtcErrorKind.other` arm context-neutralized ("Something went wrong." replaces screen-mis-specific copy that was leaking into create/send/transact contexts); deliberate no-`walletSessionProvider`-unlock-on-success decision documented inline (Task 20 owns session unlock); `_everRevealed` monotonic guard; `PopScope(canPop: _acked)`; selection + semantics tree exploit paths closed. 3 widget tests (`obscureText: true` `enterText` flutter_test caveat documented; end-to-end form-fill covered by Task 24 `fake_btc.sh` integration per L29 operator-driven). v0.2 backlog: `withTempSecretFile<T>` generic refactor (Task 6.1 follow-up closes the capture-via-closure dance), `PasswordField.clear()` explicit seam, `BtcLogFilter` configuration with `denyTokens: [_password]`, `BackupStage` sealed enum, FFI Uint8List zeroize for `OpaqueMnemonic`.
- **`[internal]`** `wallet-desktop/lib/features/wallet_import/wallet_import_screen.dart` (Task 19/26, PR #195) — Critical-tier import screen (secrets surface: BIP-39 mnemonic paste + password). Form renders Words (12/24) + `MnemonicPasteField` (Task 15: word-count validation + ack checkbox) + `PasswordField`; on submit, `withPasswordFile` (Task 5/6) routes the password via a mode-0600 temp file → `BtcCommand.walletImport` → `WalletInfo.fromJson` → `ref.invalidate(walletsListProvider)` (NOT `unawaited(notifier.refresh())` per Task 18 L12 type-design post-PR HIGH #2 — captures Task 18 lesson) → `context.go(WalletRoutes.detail(network, walletId))`. v0.1 mnemonic-in-argv: `BtcCommand.walletImport` passes `--mnemonic <words>` in argv (Task 8 design); `BtcInvoker` does NOT log argv (Task 10); `BtcLogFilter` regex covers 12-24 word-run shape for the catch path's `developer.log`. v0.2 will move to `withMnemonicFile` (Task 8 backlog) for true secret-passing parity. Deliberate no-`walletSessionProvider.unlock` on success documented inline (Task 18 L12 type-design MEDIUM #5; Task 20 will own unlock + password re-prompt on the detail screen). 1 render-only widget test (submit flow covered by Task 24 `fake_btc.sh` integration per L29 operator-driven). Re-uses Task 15 widgets + Task 17 helpers + Task 6 secret-passing without churn. v0.2 backlog: `withTempSecretFile<T>` + `withPasswordFile<T>` generic refactor (Task 6.1); `withMnemonicFile` for true mnemonic secret-passing; client-side BIP-39 checksum validation; `BtcLogFilter` configuration with `denyTokens: [_password]` bare-token bypass closure; `StrengthMeter` for password defense-in-depth.
- **`[internal]`** `wallet-desktop/lib/features/wallet_detail/wallet_detail_screen.dart` + `wallet-desktop/lib/providers/wallet_providers.dart` (Task 20/26, PR #196) — Normal-tier detail view (Stories 3+4+11+12). Password-gated unlock via `BtcCommand.walletShow` with `withPasswordFile` mode-0600 temp file → parsed `WalletDetail` → `BalanceCard` (confirmed + pending breakdown) + `AddressChip` (tap-to-copy first address) + Send / Transactions / Lock `IconButton`s in AppBar (all with `tooltip:` per L12 a11y). `walletSessionProvider(walletId).notifier.unlockWithDetail(detail)` populates the session with the parsed detail + an empty-string mnemonic sentinel ("unlocked but no mnemonic cached" — `btc wallet show` does not return the mnemonic; the CLI re-decrypts per call). Task 21 SendScreen will detect `state.mnemonic.value.isEmpty` and prompt the user to paste the mnemonic before signing. Defence-in-depth chain (L12 CRITICAL #2): password never enters argv; `BtcError` → `userMessageForBtcError` (Task 17 helper) + `StatusBadge` (Task 15); non-`BtcError` catch routes through `BtcLogFilter.redact` then `dart:developer.log` (Task 17 lesson); `_password = ''` force-cleared post-unlock + on `dispose()` (closes the mid-`withPasswordFile`-await tear-down footgun). L12 fixups applied: new `unlockWithDetail(detail)` factory encapsulates the empty-mnemonic sentinel so callers don't carry the convention (type-design HIGH); capture `walletId/network` at top of `_unlock()` + re-assert identity before `notifier.unlockWithDetail(...)` to close the mid-await widget-rebuild cross-key footgun (type-design MEDIUM); `@override dispose()` zeros `_password` (type-design LOW); `assert(isValidWalletIdSegment(widget.walletId))` in `build` (type-design LOW defence-in-depth); tooltip on Send + Transactions `IconButton`s (flutter-reviewer MEDIUM); redundant body Lock button dropped (flutter-reviewer NIT); `Network` + `Type` Text wrapped with `textTheme.bodyMedium` (flutter-reviewer NIT); key-based finders in tests (`wallet_detail_send`/`history`/`lock`) replace brittle `find.text('Lock')` (pr-test-analyzer MEDIUM); lock test asserts `container.read(walletSessionProvider(_kWalletId))` is null after lock (pr-test-analyzer MEDIUM); seed test asserts `mnemonic.value.isEmpty` (pr-test-analyzer LOW — documents the v0.1 sentinel contract in code); placeholder test converted to `skip: 'Task 24 integration test'` (pr-test-analyzer LOW). 4 widget tests (3 pass + 1 skipped). `dart analyze --fatal-warnings --fatal-infos` clean; full suite 91 pass + 5 skip (4 pre-existing env-dependent + 1 Task 20 deferred). v0.2 backlog: type the read-only-unlock sentinel at `WalletSession` level (e.g., `bool isUnlockedWithoutMnemonic`) so it can't collide with post-`dispose()` `value.isEmpty` (Lesson 32.1); app-lifecycle auto-lock on backgrounding (paused/hidden/detached); `Network` enum + `WalletId` value class (cross-cutting refactor across Tasks 13/14/15/16/17/20); FFI Uint8List + Finalizable for real zeroization.
- **`[internal]`** `wallet-desktop/lib/features/wallet_send/send_screen.dart` + `wallet-desktop/lib/core/btc/btc_command.dart` + `wallet-desktop/lib/widgets/mnemonic_paste_field.dart` (Task 21/26, PR #197) — Critical-tier broadcast screen (Stories 5+6). Three render branches keyed on `walletSessionProvider` state: `state == null` → LockedView (back-to-unlock via `pushReplacementNamed` to avoid stack leak), `state.mnemonic.value.isEmpty` (Task 20 sentinel) → MnemonicReentryView with `MnemonicPasteField.onSubmit` hook + `_onReentrySubmit` → `notifier.unlock(mnemonic: x, detail: priorDetail)` (preserves the parsed `WalletDetail`), `state.mnemonic.value.isNotEmpty` → SendForm (Address / Amount / Fee rate / Send). v0.1 mnemonic-passing workaround: `withTempSecretFile` → `BtcCommand.walletSend(passwordFilePath: path, mnemonic: '')` — `WalletSend.argv` now skips the `--mnemonic` flag when empty (L12 CRITICAL #2 invariant restoration; the prior implementation emitted `--mnemonic ''` even when the mnemonic was piped via `passwordFilePath`). Mainnet confirm gate: `widget.network == 'bitcoin'` → `AlertDialog` requiring user to type `yes` (exact-match after `trim`); the `_withConfirm` helper attaches `confirmYes` only on mainnet so testnet/signet/testnet4 don't carry a null flag. Defence-in-depth chain (L12 CRITICAL #2): mnemonic wrapped in `withTempSecretFile` mode-0600 + auto-unlink (Task 5/6) + never enters argv (Task 21 L12 fix); `BtcInvoker` strips env (Task 10) + doesn't log argv; `BtcLogFilter.redact` on non-`BtcError` catch (Task 7); `BtcError` → `userMessageForBtcError` + `StatusBadge` (Task 17 helper); `ref.listenManual` clears in-flight flags (`_running`, `_feeRateEdited`, `_error`, `_result`) on external `lock()` from another screen (detail-screen lock button) — closes the deep-link race where a stale `_running` could survive across sessions. L12 fixups applied (3 sub-agents — flutter-reviewer + type-design-analyzer + security-auditor; all returned BLOCK on first pass): pure `build()` (mnemonic + wordCount derived per build from `session.mnemonic.value`; no screen-side `_mnemonic` cache that would extend cleartext lifetime past `lock()`); `late final _feeController` with `initState`+`dispose` discipline (no per-rebuild allocation that would leak + reset cursor); `_feeRateEdited` gate so the Esplora fetch doesn't clobber user-edited fee values; `assert(isValidWalletIdSegment(widget.walletId))` in `build` (Lesson 32.3 defence-in-depth against deep-link bypass of router redirect); `MnemonicPasteField.onSubmit` + public `submit()` (Task 21 L12 fix — wires the previously-dead-end reentry view); `_onReentrySubmit` preserves `priorDetail` (the `WalletSessionNotifier.unlock(mnemonic: x)` would otherwise drop `detail` to null per the type-design Task 21 HIGH finding). 4 widget tests (3 pass + 1 skipped to Task 24). `dart analyze --fatal-warnings --fatal-infos` clean; full suite 94 pass + 6 skip. v0.2 backlog: `MnemonicChannel` sealed class for true mnemonic-file parity (Task 8 backlog — replaces the v0.1 passwordFile-path workaround with a typed channel); `OpaqueMnemonic.sentinel` typed sentinel + `WalletSession.isUnlockedWithoutMnemonic` flag (Lesson 32.1); FFI Uint8List + Finalizable for real zeroization (F47); `testBtcInvoker()` named constructor on `BtcInvoker` returning a `binaryPath: 'echo'` stub for tests (Lesson 33.5); `Address` / `AmountSats` value types (type-design Task 21 MEDIUM follow-ups); v0.2 typed `MainnetConfirm` exact-match value class; `HookConsumerWidget` migration for controller lifecycle (Lesson 33.2 v0.2).
- **`[internal]`** `wallet-desktop/lib/features/wallet_transactions/transactions_screen.dart` + `wallet-desktop/lib/widgets/mnemonic_paste_field.dart` (Task 22/26, PR #198) — Normal-tier read-only tx-history screen (Story 7). Three render branches keyed on `walletSessionProvider` state: `state == null` → LockedView (back-to-unlock via `pushReplacementNamed` — no stack leak per L33.2 from Task 21), `state.mnemonic.value.isEmpty` (Task 20 sentinel) → MnemonicReentryView with `MnemonicPasteField.onSubmit` hook + `_onReentrySubmit` (preserves the parsed `WalletDetail` per Task 21 L12 fix), `state.mnemonic.value.isNotEmpty` → TxListView (`btc tx-list --mnemonic <words> --json`). v0.1 mnemonic-in-argv gap documented (Task 8 backlog — stdin-mode for mnemonic-only CLI calls). Defence-in-depth chain: `BtcInvoker` strips env (Task 10) + doesn't log argv; `BtcLogFilter.redact` on non-`BtcError` catch (Task 7); `BtcError` → `userMessageForBtcError` + `StatusBadge` (Task 17 helper); `ref.listenManual` clears in-flight state on external `lock()` from another screen. L12 fixups applied (3 sub-agents — flutter-reviewer + type-design-analyzer + pr-test-analyzer): **HIGH** — defensive `j is List` parse check (matches Task 17 pattern; `(j as List)` threw on `parse(null)` from empty-stdout `BtcInvoker`, surfacing "Something went wrong." for empty wallets); **HIGH** — L32.2 identity re-assert added to catch + finally branches of `_load` (not just pre-await — `mounted` check covers disposal but NOT widget-property-change while still mounted); **HIGH** — LockedView path + BtcError surface paths now covered by tests; **MEDIUM** — `GlobalKey<MnemonicPasteFieldState>` typed access (had to expose `MnemonicPasteFieldState` publicly; `_MnemonicPasteFieldState` was private — underscore-prefixed State classes that need cross-widget access must drop the prefix); **MEDIUM** — expanded comment on hardcoded `expectedWordCount: 12` to surface severity (silently rejects 24-word wallets). Lessons applied: L33.1 pure `build()`, L32.2 identity capture-then-re-assert, L32.3 `assert(isValidWalletIdSegment)`, L33.3 `MnemonicPasteField.onSubmit` wired, L33.5 `btcInvokerProvider` + `esploraConfigProvider` test overrides. 6 widget tests (5 pass + 1 skipped to Task 24). `dart analyze --fatal-warnings --fatal-infos` clean; full suite 99 pass + 7 skip. v0.2 backlog: stdin-mode for mnemonic-only CLI calls (Task 8 backlog); typed `TransactionsScreenView` sealed class for exhaustive switch (type-design Task 22 NIT); `TxDirection.label` extension for friendly row labels; limit dropdown (10/50/100/500); custom_lint rule flagging `(j as List)` casts inside `parse:` callbacks (Lesson 34.1); custom_lint rule flagging async-await methods that read `widget.X` after `await` without re-assert guard (Lesson 34.2); v0.2 sweep of Tasks 20/21 to apply L34 lessons retroactively.
- **`[internal]`** `wallet-desktop/lib/features/settings/settings_screen.dart` + `wallet-desktop/lib/providers/esplora_config_provider.dart` (Task 23/26, PR #199) — Normal-tier SettingsScreen. Reads + writes `EsploraConfig{network, url, spkiPin}` via `esploraConfigProvider` (Task 12 AsyncNotifier). Network picker (bitcoin / testnet / testnet4 / signet / regtest) + URL field + SPKI pin field + Save button. Defence-in-depth patterns applied: try/catch around `save()` (silent-failure UX bug — user thinks config persisted when disk write failed; false-positive confirmation hides the failure); `EsploraConfig.defaults` now covers all 5 networks (regtest dropdown option previously fell through to invalid empty-URL state because `defaults` had no regtest case — the `default:` branch returned `url: ''`); known-network allowlist in `initState` postFrameCallback defends against hand-edited config files or schema drift (unknown network falls back to `'testnet'` instead of crashing the dropdown). Lessons applied: L33.1 pure `build()`; L33.2 controller hoist (`late final _urlController` + `late final _pinController` allocated in `initState` + disposed in `dispose`); L34.1 typed defaults; L21 redacted-error pattern (`developer.log` on save failure, no `BtcError` here since local file IO not CLI). 2 widget tests with conditional disk-roundtrip file-write assertion (SnackBar lifecycle in `flutter_test` is timing-sensitive; file assertion covers the load-bearing disk-write path; v0.2 follow-up: explicit `find.byType(SnackBar)` + delay pump). `dart analyze --fatal-warnings --fatal-infos` clean; full suite 101 pass + 7 skip. v0.2 backlog: typed `EsploraUrl` / `SpkiPinHex` value classes with F36 HTTPS-only + F20 hex-shape validation (form-level); `Network` enum (cross-cutting refactor across Tasks 13/15/16/17/20/22/23 — eliminates the missing-case class of bug at the type level); `userEditedUrl/Pin` flag to gate auto-derive on network change (prevents clobbering self-hosted Esplora configs); explicit SnackBar assertion test + dropdown-change test (pr-tester Task 23 HIGH); custom_lint rule flagging `provider.notifier.save()` without try/catch (Lesson 35.1).
- **`[internal]`** `wallet-desktop/test/integration/fixtures/fake_btc.sh` + `wallet-desktop/test/integration/fixtures/with_secret_env.sh` + `wallet-desktop/scripts/build_fake_btc.sh` + `wallet-desktop/test/integration/wallet_lifecycle_test.dart` + `wallet-desktop/test/unit/btc_invoker_test.dart` + `wallet-desktop/.gitignore` (Task 24/26, PR #200) — Normal-tier integration test infrastructure. Hermetic bash `fake_btc.sh` fixture emulates the `btc` CLI surface (argv dispatch for `wallet list|show|create|import|send|delete|sync|balance|bump-fee` + `tx-list` + `fee-estimates` + `config show`) for Dart-side `BtcInvoker` integration tests. L12 CRITICAL #2 invariant enforced (no mnemonic/password echoes; canned `abandon ... about` BIP-39 phrase for create/import; canonical mnemonic asserted on the round-trip); L7 env-strip at fixture boundary (filtered `.last_env` write — strips `BTC_WALLET_MNEMONIC`/`BTC_ENCRYPT_PASSWORD`/`BTC_DECRYPT_PASSWORD`); exit-code mapping per spec §4.3 (0 success / 1 unknown / 2 wrong-password). `with_secret_env.sh` wrapper exports `BTC_WALLET_MNEMONIC=probe-secret-must-be-stripped-by-L7` then execs the fixture — positive-control env-strip test (L36.5; Dart 3+ `Platform.environment` is unmodifiable, so the secret must be set by a shell wrapper). `build_fake_btc.sh` copies the fixture to `build/fake_btc/btc` with chmod +x for operator PATH-override smoke. `wallet_lifecycle_test.dart` 7 e2e tests driving `BtcInvoker` against the fixture: list/show/tx-list/create/import/fee-estimates/send-dry-run. `btc_invoker_test.dart` surgical L34.1 fix to test #1 parse callback (latent bug surfaced by fixture build — `(j as List).first as String` cast on a `Map`); env-strip test reworked to use `with_secret_env.sh` wrapper (L12 reviewer HIGH: previous version was vacuously true — never set the secret, so the assertion always passed regardless of BtcInvoker's filter). `.gitignore` extended to exclude `test/integration/fixtures/.last_env` (runtime artifact). L12 fixups applied (3 sub-agents — flutter-reviewer + pr-test-analyzer + silent-failure-hunter; flutter-reviewer verdict BLOCK, all HIGH/MEDIUM/LOW addressed): **HIGH** — env-strip vacuously true → wrapper-based test (Lesson 36.5); **HIGH** — `tx-list mnemonic: ''` perpetuated L33.4 → non-empty fake mnemonic (`fake-mnemonic-not-echoed-by-fixture` — fixture never echoes this); **MEDIUM** — L34.1 `is Map<String, dynamic>` guards on 4 parse callbacks (show/create/fee-estimates/send); **MEDIUM** — canonical `abandon ... about` mnemonic pinned on create+import (L12 CRITICAL #2 invariant — any other mnemonic value fails loudly); **MEDIUM** — dropped `BTC_ESPLORA_SPKI_PIN` from grep filter (over-stripping vs actual `_secretEnvKeys` contract); **LOW** — bash `*)` catch-all re-ordered to last position (regression caught at verify gate — misplaced `*)` won first-match and broke 5 of 7 tests; Lesson 36.1); **LOW** — env-write `set -e` block + assert `.last_env` exists; **LOW** — absolute `mockScript` path via `p.canonicalize` (CWD-independent). Lessons applied (5 new — all Task 24 contributions): L36.1 bash `case` ordering (first-match-wins); L36.2 `throw const FormatException` is a Dart compile error; L36.3 `BtcCommand`'s static factory methods are not const-constructors (`// ignore_for_file: prefer_const_constructors` required in test files); L36.4 L34.1+L33.4 must propagate to test-side parse callbacks (tests bake precedent); L36.5 Dart 3+ `Platform.environment` unmodifiable — env-strip tests need shell wrapper. 7 new test files (6 wallet_lifecycle + 3 btc_invoker activated from skip-protected). `dart analyze --fatal-warnings --fatal-infos` clean; full suite 111 pass + 4 skip (was 101/7 pre-Task-24). Plan progress: 24/26 (92.3%).
- **`[internal]`** `.github/workflows/wallet-desktop-ci.yml` + `.github/workflows/btc-bundle.yml` (Task 25/26, PR #201) — Trivial-tier CI workflows (per L31). `wallet-desktop-ci.yml`: Flutter desktop UI CI for `wallet-desktop/`. Path filter on `wallet-desktop/**` + this workflow file. Steps: `flutter pub get` → `dart analyze --fatal-warnings --fatal-infos` → `flutter test --coverage` → codecov upload. Cancel-in-progress + concurrency group follows existing `ci.yml` pattern. Real-btc Rust CI in `ci.yml` already covers Rust side. `btc-bundle.yml`: cross-build release bundle for the `btc` CLI binary on semver tags (`v*.*.*`) or manual `workflow_dispatch`. 5-target matrix (`x86_64-unknown-linux-gnu` / `aarch64-unknown-linux-gnu` / `x86_64-apple-darwin` / `aarch64-apple-darwin` / `x86_64-pc-windows-msvc`) using `dtolnay/rust-toolchain` + `Swatinem/rust-cache` + `cargo build --release --target`. Per-target artifact uploaded via `actions/upload-artifact@v4`. Plan drifts corrected: `flutter-btc-wallet/` → `wallet-desktop/` (repo rename per Tasks 1-3); dropped redundant `Build btc (host)` + `Copy btc → test fixture` steps (Rust CI covers btc-cli build; `wallet_lifecycle_test.dart` Task 24 uses the fake fixture, not the real binary); action SHA hygiene (3 of 7 SHAs replaced with tag-based references for unverified actions — to be pinned after first successful CI run). Lesson L37 captures the SHA hygiene pattern. Trivial-tier per L31: skip TDD + L12 review; verify gate only (YAML parse valid + action SHA audit applied). `dart analyze` unchanged (config-only). Plan progress: 25/26 (96.2%).

### User Stories

| Story | Status | Notes |
|---|---|---|
| 1 Create | ✅ | v0.2.0 FFI path (Task 11 PR #244): `walletCore.createWallet` via `MnemonicHandle` — F47 zeroization gap closed (Rust `Secret<String>` never crosses FFI as Dart `String`) |
| 2 Import | ✅ | v0.2.0 FFI path (Task 12 PR #245): `walletCore.importWallet` via `SecretBuffer` for mnemonic + password |
| 3 Balance | ✅ | v0.2.0 FFI path (Task 13 PR #246): `wallet_show` — `Balance` collapsed to single `confirmedSat` (v0.2.1 restores 4-tuple once Esplora sync wires) |
| 4 Sync | ✅ | Deferred to v0.2.1 — Rust `peek_addresses` requires bdk sync; v0.2.0 shows empty `firstAddress` |
| 5 Send | ✅ | v0.1.0 CLI path (Task 21); v0.2.0 FFI migration deferred (Task 14 sub-split A+B required) |
| 6 Fee rate | ✅ | Default 1 sat/vB; manual override (Task 21 fee-rate field); v0.2.0 FFI migration deferred (Task 16) |
| 7 Tx history | ✅ | v0.1.0 CLI path (Task 22); v0.2.0 FFI migration deferred (Task 15) |
| 9 List/show | ✅ | v0.2.0 FFI path (Tasks 10+13 PRs #243 + #246): `walletsListProvider` reads `walletCore.listWallets`; detail screen via `wallet_show`. btc CLI plumbing removed (Task 13 + SHA `f04d695`) |
| 11 Config | ✅ | v0.1.0 path (Task 23) — SettingsScreen Esplora URL + SPKI pin persistence |
| 12 Persist | ✅ | Reuses `btc --data-dir` (v0.1.0); v0.2.0 reuses native FFI db-path |
| 20 Address type | ✅ | v0.2.0 FFI path (Task 11): 4 types via `walletCore.createWallet({addressType: FfiAddressType})` |

### Try it (v0.2.0 — operator-driven per L29)

**Build the native cdylib** (replaces the deleted `Build fake_btc` CI step):

```bash
cd rust-wallet-app
cargo build --release -p bitcoin-wallet-core
mkdir -p ../wallet-desktop/native/linux-x64
cp target/release/libbitcoin_wallet_core.so \
   ../wallet-desktop/native/linux-x64/librust_wallet_core.so
```

(Platform-specific: `librust_wallet_core.dylib` on macOS, `rust_wallet_core.dll` on Windows. See `wallet-desktop/lib/core/ffi/native_lib.dart`.)

**Run the Flutter UI**:

```bash
cd wallet-desktop
flutter pub get
flutter test                                  # 200+ tests pass + skip-where-platform-gated
flutter run -d linux                         # desktop window with WalletCreate / List / Detail
```

**Verify FFI migration** (smoke checks for each completed screen):

```bash
cargo test -p bitcoin-wallet-core ffi::wallet_ops::tests::wallet_show_after_create_returns_matching_metadata
cargo test -p bitcoin-wallet-core ffi::wallet_ops::tests::wallet_show_wrong_password_returns_wallet_store
```

**Live testnet smoke** (operator-driven per L29 — requires real testnet Esplora):

```bash
# 1. Open wallet-desktop → Create new testnet wallet
# 2. Confirm: wallet file appears in ~/.local/share/wallet-desktop/wallet_data/testnet/
# 3. Fund via faucet (https://testnet-faucet.com/)
# 4. Wait 1 confirmation, refresh detail screen
# 5. (Task 14 deferred) SendScreen still uses v0.1.0 CLI path
```

### Removed

- `wallet-desktop/scripts/smoke/UI_TEST_CHECKLIST.md`: stale pre-FFI subprocess references removed (`ProcessProgressOverlay — spinner during CLI invocation`, `Verify L7 env-strip`, `Real btc binary`, `Sandbox-L29 substitute (chrome-devtools-mcp + fake_btc.sh)` section) per Issue #259 drift. Title bumped v0.1.0 → v0.2.x; Run block now references PR #257 auto-screenshot capture. (PR #260)

### Fixed

- L12 CRITICAL #2 (mnemonic + password redaction) defended at multiple layers: `BtcLogFilter` (Task 7) scrubs `package:logging` output; `BtcInvoker` strips secret env vars + never logs argv (Task 10); `TempSecretFile` mode-0600 + auto-unlink (Task 5); `withPasswordFile` (Task 6) routes passwords via temp file rather than argv.
- 5 L12 review lessons (L32.1–L35.3) captured + applied across Tasks 20-23 for defense-in-depth patterns: capture-identity-before-await, sentinel-at-API-surface, pure-build, hoist-controllers, defensive-parse-null, save-try-catch, defaults-cover-dropdown.

### Security

- **L12 CRITICAL #2** (mnemonic + password never logged) — defended at 4 layers (Task 7 `BtcLogFilter`, Task 10 `BtcInvoker` env-strip, Task 5 `TempSecretFile` 0o600 + auto-unlink, screen-side `_password = ''` clear).
- **L7 env-strip** — `BtcInvoker` strips `BTC_WALLET_MNEMONIC` / `BTC_ENCRYPT_PASSWORD` / `BTC_DECRYPT_PASSWORD` before child process spawn; verified by Task 24 `with_secret_env.sh` fixture.
- **L12 CRITICAL #2 (test-side)** — Task 24 fake fixture pins canonical `abandon ... about` mnemonic; any other value fails loudly.

### Operator-driven (L29)

- Live testnet smoke (fund via faucet, send 0.001 BTC, verify) is **operator-driven**, NOT in CI. Run script documented in `wallet-desktop/README.md` (post-release follow-up).
- **`[internal]`** `Wallet::persist()` + `Wallet::load_persisted()` + `db_path: Option<PathBuf>` field (Story 12 / Issue #130 PR3 lib layer, PR #134 SHA `f30d47c` + `e522e78`) — bdk_file_store 0.22 SQLite-like append-only file captures the wallet's `ChangeSet` (network, descriptors, chain index, tx graph) between CLI invocations. `Wallet::persist()` writes the current `ChangeSet` to the configured `db_path`; `Wallet::load_persisted()` reads the aggregated `ChangeSet` and returns a fresh `Wallet` (caller invokes `sync()` after to rebuild the tx graph from Esplora). F14 persistence atomicity (bdk_file_store fsyncs via `Drop` on the `Store` handle). F34 word-count validation in `load_persisted`. CLI wiring (`--db-path <PATH>` flag on `btc wallet show` / `wallet create`) deferred to a follow-up PR. 14 existing call sites updated to pass `None` (in-memory mode, unchanged behavior). `cargo test --lib`: 264 passed, 0 failed.
- **`[user-facing]`** `btc tx-list --mnemonic <words> --network <NET> --esplora-url <URL> [--pin-spki <hex64>] [--limit N] [--json]` CLI subcommand (Story 7 / Issue #120 / Task 27 `chain::explorer`) — read-only Esplora scan + tx graph enumeration. Lists deduped txids + block-explorer URLs (blockstream / mempool) for the wallet's keychain. CLI command is `tx-list` (kebab-case) to avoid clap-subcommand conflict with any future `tx <sub>` siblings. Reuses `build_esplora_client_for` for F20 SPKI + F36 HTTPS-only enforcement. New `bitcoin_wallet_core::chain::explorer::{tx_url, address_url}` (3 lib tests) + `Wallet::txids()` method (returns sorted, deduped txids from bdk's tx graph). 3 CLI parse tests (accept + limit/json flags + debug-redaction). Closes v0.1.1 Story 7.
- **`[user-facing]`** `btc fee-estimates [--network N] [--esplora-url URL] [--pin-spki HEX64] [--json]` CLI subcommand (Story 8 / Issue #121 / Task 14 `get_fee_estimates` portion) — read-only Esplora fee estimator. No wallet required (uses the `bitcoin_wallet_core::chain::esplora::EsploraClient::fee_estimate()` HTTP client directly). Pretty table by default (`target_blocks | sat/vB`, sorted ascending); `--json` for machine-readable output. Reuses `default_url_for` + `parse_spki_pin_hex` + `build_esplora_client_for`-style client construction (F20 SPKI pin + F36 HTTPS-only enforcement). Output includes `network` label; empty maps surface `(no estimates)` (defensive against Esplora incidents). 3 CLI parse tests (accept + `--json` flag + reject missing network). Closes v0.1.1 Story 8.
- **`[user-facing]`** `btc wallet send --fee-rate <SAT_PER_VB>` CLI flag (Story 6 / Issue #119 / Task 14) — user-specified sat/vB fee rate. Validates `>= 1` (0 = no fee, txs don't relay). Default (`None`): 1 sat/vB (conservative; Story 8 #121 will fetch Esplora estimates when omitted). `Wallet::send` signature gains `fee_rate: FeeRate` parameter — callers must construct explicitly (no internal default). Threat-model: invalid fee rate → `anyhow!` error before any network IO. 2 new CLI parse tests + 1 debug-redaction test.
- **`[user-facing]`** `btc wallet send --mnemonic <words> --network <NET> --address <ADDR> --amount-sat <SAT> --esplora-url <URL> [--pin-spki <hex64>]` CLI subcommand (Issue #118 / Story 5 / Task 11+13) — full tx lifecycle: sync → build → sign → broadcast → return txid. Default fee rate 1 sat/vB (Story 6 #119 added `--fee-rate` override). Threat-model: cross-network rejection enforced via `Address::require_network` (defends against "send to wrong chain" operator error); F36 HTTPS-only Esplora URL reuses `EsploraUrl::new` validator (Issue #63); F20 SPKI pin enforcement reuses `build_esplora_client_for` (non-regtest requires pin); L12 CRITICAL #2 mnemonic redaction in Debug (mirrors `Sync`/`Balance` pattern). 4 lib unit tests + 3 CLI integration tests + 4 demo-script gates cover accept/reject/cross-network/invalid-address/F36. Closes Phase 1 §MVP follow-up gap.
- **`[user-facing]`** `btc wallet send --to <addr>:<amount>` (Story 13 / Issue #131 part A / Issue #138) — multi-recipient send. Repeatable flag, up to MAX_RECIPIENTS=20 entries (BDK recommended safe max, enforced at handler layer). Cross-network rejection per recipient via `require_network`. Output: `sent. txid: <txid> (recipients: N)`. New lib layer: `tx::builder::build_multi_recipient_tx(&mut BdkWallet, &[(Address, Amount)], FeeRate) -> Result<Psbt>` + `Wallet::send_multi` + `Wallet::build_sign_multi`. Mutually exclusive with `--address`/`--amount-sat`/`--drain-to` at parse layer. Closes v0.1.1 Story 13.
- **`[user-facing]`** `btc wallet send --drain-to <addr> [--exclude-utxo <txid:vout>]` (Story 14 / Issue #131 part A / Issue #138) — drain all spendable UTXOs to a single destination. `--exclude-utxo` is repeatable; locked UTXOs are skipped via bdk's `add_unspendable`. Exits 4 when no spendable UTXOs remain after exclusions. New lib layer: `tx::builder::build_drain_tx(&mut BdkWallet, &Address, FeeRate, &[OutPoint]) -> Result<Psbt>` + `Wallet::send_drain` + `Wallet::build_sign_drain`. Closes v0.1.1 Story 14.
- **`[user-facing]`** `btc wallet send --dry-run` (Stories 13+14 / Issue #138) — build + sign without broadcasting. Output is a base64-encoded signed PSBT (live key material — anyone who captures it can broadcast the spend). STDERR banner preceeds STDOUT output per L12 sec-H2 (F25). Dry-run paths (`build_sign_multi` / `build_sign_drain`) perform the same `balance()` lazy-sync as `send_*` so the PSBT references the live UTXO set (no stale-state signed txs).
- **`[security]`** F25 xprv-leak defense tightened (L12 sec-M1): `sanitize_create_tx_error` no longer interpolates bdk's `Display` output. Each variant emits a fixed variant name only — `CoinSelection(InsufficientFunds)`, `NoUtxosSelected`, `Descriptor`, `Policy`, etc. — eliminating the descriptor-echo surface in `CreateTxError::Descriptor` / `Policy` Display impls. Both `build_send_tx` (pre-existing) and the new `build_multi_recipient_tx` / `build_drain_tx` route through the tightened sanitize.
- **`[security]`** Mainnet send gate extended to Stories 13+14 (L12 sec-H1): `--network mainnet` non-dry-run sends require `--confirm-yes yes` (Story 10 precedent). Defends against operator typo draining real BTC via `--drain-to` / `--to`. Dry-run exempt (no broadcast).
- **`[security]`** F25 PSBT-broadcast warning (L12 sec-H2): every dry-run path emits a STDERR warning before printing the base64-encoded signed PSBT to STDOUT. The PSBT is fully signed; capturing it is equivalent to capturing the spend authority.

### Sub-task B — Stories 15+16 send extensions (Issue #131 part B / Issue #139 / PR sub-task B)

- **`[user-facing]`** `btc wallet send --coin-selection bnb|knapsack|oldest` (Story 15 / Issue #139) — coin-selection algorithm menu. Default `bnb` (BDK default = `BranchAndBoundCoinSelection<SingleRandomDraw>`). `knapsack` maps to `LargestFirstCoinSelection` (greedy largest-first; bdk 3.1 has no standalone `Knapsack` impl — closest semantic equivalent). `oldest` maps to `OldestFirstCoinSelection` (picks oldest-block UTXOs first). **Drift documented:** user-stories spec lists these three labels verbatim; bdk 3.1 has no `Knapsack` impl. Single-recipient mode only; `--drain-to` and `--to` mutually exclusive at parse layer (per L12 H1 + H3 fix — silently dropping the flag in those modes was a documented-but-false contract). New lib layer: `tx::builder::CoinSelection` enum + `build_send_with_coin_selection`. Closes v0.1.1 Story 15.
- **`[user-facing]`** `btc wallet send --input <txid:vout>` (Story 16 / Issue #139) — manual UTXO selection. Repeatable; one or more `<txid>:<vout>` outpoints the operator wants to fund the send with. Outpoints not tracked by the wallet fail at build time with sanitized `"add_utxo failed: UnknownUtxo"` (L12 L1 fix). When `--manual-selection-only` is set (requires `--input` per L12 M2), bdk's `manually_selected_only` strict mode applies (fails if selected UTXOs don't cover amount + fee; no auto-append). `--input` + `--coin-selection` together: both honored — manual UTXOs are always included, algorithm applies to any auto-fill (L12 H3 fix). New lib layer: `build_send_with_manual_utxo(&mut BdkWallet, &Address, Amount, FeeRate, &[OutPoint], bool, CoinSelection) -> Result<Psbt>` + `Wallet::send_with_manual_utxo` + `Wallet::build_sign_with_manual_utxo`. Closes v0.1.1 Story 16.
- **`[security]`** Parse-layer guards for Stories 15+16 (L12 H1 + H3 + M2 fixes): `--coin-selection` / `--input` / `--manual-selection-only` all conflict with `--drain-to` and `--to` (single-recipient mode only). `--manual-selection-only` requires `--input` (silent no-op was wrong default for a spend command). `parse_outpoint_arg` parameterized to flag-aware error messages (was hardcoded `--exclude-utxo`).

### Sub-task C — Story 17 RBF bump-fee (Issue #131 part C / Issue #140 / PR sub-task C)

- **`[user-facing]`** `btc wallet bump-fee --txid <id> --fee-rate <sat/vb>` CLI subcommand (Story 17 / Issue #140) — RBF fee bump. Replaces an unconfirmed tx with one paying a strictly higher fee. Uses bdk 3.1 `build_fee_bump(txid)` (BIP-125 sequence auto-bumped). Lib layer enforces `fee_rate > original_fee_rate` (RBF rule 3 — replacement must pay strictly more). New lib layer: `tx::builder::build_bump_fee_tx(&mut BdkWallet, Txid, FeeRate) -> Result<Psbt>` + `Wallet::bump_fee` + `Wallet::build_sign_bump_fee` (dry-run, base64 PSBT). `--dry-run` builds + signs without broadcasting. `--confirm-yes yes` required for mainnet non-dry-run (Story 10 precedent extended). Closes v0.1.1 Story 17.

### Story 20 / Issue #132 — pick address type on creation

- **`[user-facing]`** `btc wallet create --type <legacy|nested-segwit|native-segwit|taproot>` (Story 20 / Issue #132) — pick address type at creation. Default `native-segwit` (BIP-84 P2WPKH, unchanged). Other types opt-in: `legacy` (BIP-44 P2PKH `m`/`n`), `nested-segwit` (BIP-49 P2SH-P2WPKH `2`), `taproot` (BIP-86 P2TR `tb1p...`). New lib layer: `AddressType::receiving_template()` + `AddressType::change_template()` (descriptor wrappers `pkh({key}/*)`, `sh(wpkh({key}/*))`, `wpkh({key}/*)`, `tr({key}/*)`); `Wallet::from_mnemonic_with_type` constructor; `Wallet::build_bdk_wallet` parameterized by `address_type` (was hardcoded `NativeSegwit`); `create_wallet` gains `address_type` parameter. Sidecar metadata file `<wallet-id>.type` (one-line ASCII label + newline, atomic write via tempfile + rename per F19) preserves the type on disk; `show_wallet` reads it via `read_address_type_or_default` and reconstructs the correct descriptor. Back-compat: wallets created before this PR have no sidecar → default `native-segwit` (no migration). New CLI enum `AddressTypeArg` mirrors lib `AddressType` (clap `ValueEnum` lives in binary crate per the same separation pattern as `CoinSelection` in sub-task B). 4 CLI parse tests + 2 lib template tests + 1 roundtrip integration via existing `handle_show`. Closes v0.1.1 Story 20.
- **`[internal]`** `bitcoin-wallet-core::tx` module — three single-purpose units wrapping `bdk_wallet::TxBuilder` + `wallet.sign` + Esplora broadcast: `tx::builder::build_send_tx(&mut BdkWallet, &Address, Amount, FeeRate) -> Result<Psbt>`, `tx::sign::sign_psbt(&BdkWallet, &mut Psbt) + extract_tx(&Psbt) -> Transaction`, `tx::broadcast::broadcast(&EsploraClient, &Transaction) -> Result<Txid>`. Sanitized error mapping (bdk `CreateTxError` variant names preserved without descriptor echo per F25 / U1). 3 lib unit tests pin the public API.
- **`[internal]`** `EsploraClient::broadcast_tx(&str) -> Result<Txid>` — POST raw tx hex to Esplora `/tx` endpoint. F20 enforcement carried via the underlying `reqwest::Client` (no separate TLS check). Sanitized error: surfaces HTTP status + first 200 chars of body on failure.
- **`[internal]`** `Wallet::send(&EsploraClient, &Address, Amount, FeeRate) -> Result<Txid>` (Story 6 / Issue #119 signature update) — composes `tx::{builder,sign,broadcast}` after lazy sync. `MutexGuard` discipline: bdk wallet is `take()`-en out for the build step (which needs `&mut`), put back BEFORE the async broadcast (no guard crosses `.await`). Story 6 replaced the internal `DEFAULT_FEE_RATE_SAT_PER_VB` constant with caller-provided `FeeRate` — CLI default lives in `handle_wallet_send`.
- **`[internal]`** `scripts/btc-send-demo.sh` — operator-friendly demo for `btc wallet send` covering 4 gates (CLI flag surface, cross-network rejection, invalid-address rejection, F36 HTTPS-only). Mirrors the gate pattern of `btc-balance-demo.sh` / `btc-import-demo.sh`.

- **`[user-facing]`** `btc wallet import --mnemonic "..." --network <NET> --password <PW>` CLI subcommand (Issue #99 / Story 2) — Phase 1 closure gap closure. Imports an existing BIP-39 mnemonic (12/15/18/21/24 words), encrypts via F5/F6 (Argon2id + AES-256-GCM), persists to `$XDG_DATA_HOME/btc/wallets/<network>/<id>.enc` per ADR 0001, prints `wallet_id` on STDOUT. Optional `--passphrase` accepted (BIP-39 derivation-time only — NOT persisted, re-supply at `wallet show` time). Threat-model: invalid checksum / unsupported word count → exit non-zero with `invalid mnemonic` error; mnemonic never echoed on STDOUT or STDERR (L28 regression coverage); Manual `Debug` redacts `mnemonic` + `passphrase` + `password` (L12 CRITICAL #2); encryption AAD bound to network discriminant (cross-network decrypt fails). 5 lib unit tests + 6 CLI integration tests cover accept/reject/determinism/non-echo/distinct-IDs/persistence. Closes Phase 1 §MVP scope Story 2 gap.
- **`[user-facing]`** `btc encrypt` / `btc decrypt` accept `--password-file <PATH>` and `--password-stdin` (Issue #84) — closes the scripted-automation gap from PR #62 backlog. Use cases: k8s secrets mounted as files, systemd `LoadCredential=`, vault-agent sidecar pipes, CI pipelines. Priority order: `--password` flag > `--password-file` > `--password-stdin` > `BTC_ENCRYPT_PASSWORD` / `BTC_DECRYPT_PASSWORD` env > `/dev/tty` prompt. Mutually exclusive at parse layer via clap `conflicts_with_all`. Threat-model: `--password-file` rejects symlinks (F19 pattern) and world/group-readable files (`mode & 0o077 != 0`) — defense-in-depth so a misconfigured k8s mount can't leak the secret via `/proc/<pid>/cmdline` chain. Manual `Debug` redacts both `password` and `password_file` paths (L12 CRITICAL #2). 19 new unit tests cover parse + F20-style mode check + symlink check + priority order + end-to-end roundtrip via `--password-file`.

- **`[user-facing]`** `btc wallet create` + `btc wallet show` CLI subcommands (Task 54d, PR-2 of #64) — clap 4 subcommands wired on top of the PR-1 lib (`create_wallet` + `show_wallet`). `create` persists an encrypted wallet to `$XDG_DATA_HOME/btc/wallets/<network>/<id>.enc` and prints `wallet_id` to STDOUT + mnemonic to STDERR (L28/F49 closure — mnemonic never on STDOUT). `show` loads + decrypts + syncs from Esplora + prints `{receive_addresses, change_addresses, balance_sat}` JSON. Manual `Debug` impls on `Cli`/`Commands`/`WalletAction` redact password (L12 CRITICAL #2). Threat-model coverage reuses PR-1 defenses: F19, U6, U7, A1, A2, N2, N5, N8 — all carried through. PR #70
- **`[user-facing]`** `btc wallet show --esplora-spki-pin <HEX64>` (Issue #73, F20 enforcement) — new flag + `BTC_ESPLORA_SPKI_PIN` env to expose SPKI pin configuration for production Esplora endpoints. When set, CLI builds `EsploraClient` via `from_config` with `TlsPolicy::Pinned` (closes F20 gap on mainnet/signet/regtest). When unset, PR-2 `SystemRoots` default preserved (testnet-suitable). PR #73
- **`[user-facing]`** `btc message sign` + `btc message verify` subcommands (Issue #61 / Task 54a) — stateless BIP-137 wrapper around `crypto::bip137`. `sign` derives the first external address (`m/44'/coin'/0'/0/0`) from a BIP-39 mnemonic + network, requires the operator's `--address` to match (closes F47 mnemonic-mismatch), prints base64 signature. `verify` prints `true`/`false` for a base64 signature against an address + message. Manual `Debug` redacts mnemonic + signature (L12 CRITICAL #2 pattern). v0.1 supports legacy P2PKH only (BIP-322 / Taproot / P2WPKH deferred to v0.1.1). PR #61
- **`[user-facing]`** `btc wallet show --network <NET>` defaults to network-appropriate Esplora URL (Issue #74) — `bitcoin`→`blockstream.info/api`, `testnet`→`blockstream.info/testnet/api`, `signet`→`blockstream.info/signet/api`, `testnet4`→`mempool.space/testnet4/api`. `regtest` has no default (HTTPS-only per F20; operator must pass `--esplora-url`). PR #74
- **`[user-facing]`** `btc encrypt` + `btc decrypt` subcommands (Issue #62 / Task 54b) — stateless file encryption wrapping `crypto::mnemonic_cipher` (F5 Argon2id KDF m=256 MiB/t=10/p=4 + F6 AES-256-GCM AEAD). Reads UTF-8 `--in`, writes `MnemonicCipherBlob` (salt(16) || nonce(12) || ct || tag(16)) to `--out`; decrypt reverses. `--password` optional (env `BTC_ENCRYPT_PASSWORD`/`BTC_DECRYPT_PASSWORD` or `/dev/tty` prompt via `rpassword`). Threat-model: F47 zeroize preserved via `Secret<String>` borrow (no clone into non-zeroizing String); F19 atomic_write (`util::atomic_write`) for `--out` — 0o600 perms, write-to-temp + fsync + parent fsync + rename, no partial ciphertext/plaintext on crash; N2 oracle collapsed — wrong-password / tampered / truncated / non-UTF8 all surface as uniform `decrypt failed`; `--in == --out` refused; `--in` symlinks + oversize (`> 1 MiB` encrypt, `> MAX_LEN` decrypt) rejected pre-read. PR #62
- **`[user-facing]`** `Wallet::sync(&EsploraClient)` (Task 9 #19b.2, F12) — full chain scan via Esplora `/address/{addr}/utxo` + `bdk_wallet::Wallet::insert_txout`. Caller builds `EsploraClient` with explicit `TlsPolicy` (F20 SPKI pinning). PR #55
- **`[user-facing]`** `Wallet::balance(&EsploraClient) -> Result<u64>` (Task 9 #19b.2, F13) — confirmed-only UTXO aggregation. Lazily syncs on first call; reuses cached `bdk_wallet::Wallet` thereafter. PR #55
- **`[user-facing]`** `Wallet::sync` / `Wallet::balance` API breaking change: now take `&EsploraClient` (was `&str esplora_url`). Caller must build `EsploraClient::from_config(&WalletConfig)` (which carries network + optional SPKI pin). PR #55
- **`[user-facing]`** `EsploraClient::address_utxos(&Address) -> Result<Vec<EsploraUtxo>>` + `EsploraClient::get_tx(&Txid) -> Result<bitcoin::Transaction>` — additive API used by `Wallet::sync` for F12 chain scan. PR #55

### Security

- **`[internal]`** F20 enforcement tightened in `btc wallet show` (push-sweep #2): non-regtest networks (Bitcoin, Testnet, Testnet4, Signet) now refuse to construct an EsploraClient without an explicit `--pin-spki` (or `BTC_ESPLORA_SPKI_PIN` env). Regtest retains the operator-opt-in exemption (localhost development via stunnel + SystemRoots). Closes active mainnet attack surface where `default_url_for(Network::Bitcoin) → blockstream.info/api` would route without a pin. Mirrors the F20 gate already shipped in `btc wallet sync` / `btc wallet balance` (PR #81). 5 new unit tests pin the contract across all 4 non-regtest networks + regtest exemption.
- **`[internal]`** GitHub Actions SHA-pinning (push-sweep #1): `actions/checkout`, `dtolnay/rust-toolchain`, and `Swatinem/rust-cache` are now pinned to full commit SHAs in both `ci.yml` and `btc-cli-demo.yml`. Dependabot (already configured at `.github/dependabot.yml` for `github-actions` ecosystem, weekly cadence) will auto-bump SHAs via future PRs. Closes HIGH supply-chain-pin-mismatch finding (tag rewrite attack vector).
- **`[internal]`** F12 / F13: full implementation in `Wallet::sync` / `Wallet::balance`. F19 (`atomic_write`-backed persistence) deferred for UTXO state; encrypted mnemonic blob persistence lands in v0.1 via #54d per ADR 0001 (in-memory UTXO state until next `sync`).
- **`[internal]`** `XPrvHolder::to_xprv_secret() -> Secret<String>` (replaces `to_xprv_string`; `pub(crate)`; zeroize-on-drop) — closes xprv zeroize window in descriptor construction. PR #55
- **`[internal]`** `Error::Bdk` carries fixed message; raw bdk error dropped (avoids xprv leak via descriptor echo). PR #55
- **`[internal]`** `Wallet::sync` UTXO value capped against `Amount::MAX_MONEY`; reject on overflow (DoS mitigation against malicious Esplora response). PR #55
- **`[internal]`** `Wallet::sync` / `Wallet::balance` take `&EsploraClient` (no internal `TlsPolicy::SystemRoots` default); caller is responsible for `TlsPolicy::Pinned` for production endpoints. PR #55
- **`[internal]`** `crypto::aad::Aad<'a>` newtype + `MAX_AAD_LEN` (64-byte DoS cap) + exhaustive `Aad::network(Network)` encoding (Issue #66 precursor to ADR 0001) — typed AAD closes plaintext/AAD positional swap at call site; exhaustive match prevents silent on-disk blob remapping when `bitcoin::Network` gains a new variant (caught `Network::Testnet4` on first build). `# Errors` doc blocks on `aes_gcm::encrypt`/`decrypt`; error wraps drop `aes-gcm` internal format (oracle hygiene).
- **`[internal]`** `MnemonicCipherBlob` API: `encrypt_mnemonic` / `decrypt_mnemonic` gain required `aad: Aad<'_>` parameter (breaking); `new_checked` private constructor + `MAX_LEN` upper bound (DoS mitigation); rejects empty phrases; manual `Debug` using `finish_non_exhaustive()` (closes length-leak via `tracing::debug!(?blob)`); `from_bytes` constructor for `Aad` enforces length cap. PR #28 + #66
- **`[internal]`** `btc encrypt`/`btc decrypt` hardened per L12 critical-tier review (PR #62 fix commit): F47 zeroize regression closed (`Secret<String>` borrow, no `.clone()`); `std::fs::write` → `util::atomic_write` (F19, closes non-atomic + 0o600 umask + symlink-following-write); N2 oracle collapsed (uniform `decrypt failed` for wrong-password / tampered / truncated / non-UTF8); `--in == --out` guard added; `--in` size cap (`1 MiB` encrypt, `MAX_LEN` decrypt) + symlink rejection pre-read. PR #62
- **`[internal]`** ADR 0001 (`docs/superpowers/adrs/2026-08-11-adr-0001-btc-wallet-store.md`) — `btc` wallet-store layout decision: keep F19-deferred UTXO snapshot, persist only `MnemonicCipherBlob` at `$XDG_DATA_HOME/btc/wallets/<network>/<wallet_id>.enc` (XDG on Linux/macOS, Windows deferred). Network discriminant bound via AES-GCM AAD (closes cross-network footgun). Symlink-defense on read path; constant-time padding on missing-file path (closes file-existence + timing oracles). Unblocks #64 (Task 54d).
- **`[cleanup]`** `CONTEXT.md` deleted per audit 2026-08-10. Type-system invariants (`Secret<T>`, `bip39` `zeroize` feature, `finish_non_exhaustive()` for mnemonic types) carry the security load. PR #55

### Infrastructure (Phase 1 closure)

- **`[internal]`** GitHub Actions CI workflow (`rust-wallet-app/.github/workflows/ci.yml`) — 4-job gate mirroring L13 step 11 verify: `fmt` (rustfmt check), `clippy` (`-D warnings`), `test` (`cargo test --workspace`, L29 live-testnet `#[ignore]` preserved), `geiger` (unsafe audit). SHA-pinned `actions/checkout@v4`; cargo target dir at `rust-wallet-app/target`. Triggers on push to `main` + PR to `main`. Closes Task 1 Step 10 (final unchecked plan deliverable).
- **`[internal]`** Plan-file drift sync (`docs/superpowers/plans/2026-08-05-rust-bitcoin-wallet.md`) — all 51 plan checkboxes flipped from `[ ]` to `[x]` per L30 drift fix, except Task 1 Step 10 (closes in this PR). Workspace version drift documented inline: workspace resolved to `0.2.0` after `chain-traits` umbrella scaffold was added as a workspace member predating the F33/F35 deferral decision. Resolution: accept `0.2.0`, Phase 2 plan formally adopts `chain-traits`.
- **`[internal]`** Verification gate cleared (L28): `cargo fmt --check` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo test --workspace` 89 passed / 0 failed / 1 ignored (L29 live testnet), `cargo geiger` 0 unsafe fn/impl/trait in `bitcoin-wallet-core` (6 unsafe expressions, all `Secret::into_inner` pattern per F53).

## [v0.1.0] — 2026-08-10

### Added

- **`[user-facing]`** Workspace scaffold (`bitcoin-wallet-core` library + `btc` CLI) — PR #11
- **`[internal]`** Threat model spec (F1–F53, A1–A8, U1–U7, T1–T4) — PR #10
- **`[internal]`** `Secret<T>` newtype (F47 zeroize wrapper) — PR #23
- **`[internal]`** `atomic_write` + 0o600 permissions (defends U6, U7) — PR #23
- **`[internal]`** 17-variant `Error` enum — PR #13
- **`[user-facing]`** `keys::mnemonic` BIP-39 (`Secret<bip39::Mnemonic>`, zeroize-on-drop) — PR #25
- **`[user-facing]`** `keys::derivation` + `keys::signer` (BIP-32 + secp256k1) — PR #26
- **`[user-facing]`** `crypto::argon2` KDF + `crypto::aes_gcm` encryption (F5, F6) — PR #27
- **`[user-facing]`** `crypto::bip137` message signing + verification (F7, F9, F50) — PR #33
- **`[user-facing]`** `chain::spki` SPKI pin primitives + `chain::esplora` SPKI-pinned TLS (F20) — PR #34
- **`[user-facing]`** `chain::network::coin_type_for` (F37 BIP-44 coin type) — PR #42

### Changed

- **`[internal]`** `Signer::sign_recoverable` signature changed from `&[u8; 32]` to `&MessageHash<Bip137Message>` (F21 type-level defense, U5 phishing mitigation). Caller wrapping in `bip137::sign_message` is internal; public API unchanged — PR #39

### Security

- **`[internal]`** F20 (SPKI pinning for Esplora TLS) — Task 7 — PR #34
- **`[internal]`** F21 (typed Sighash defense for `sign_recoverable`) — U5 mitigation at compile time, sealed trait + phantom-typed wrapper; public API unchanged — PR #39
- **`[internal]`** F37 (BIP-44 coin-type derivation) — defense against caller-supplied-wrong-coin-type footgun; public API addition via `coin_type_for` — PR #42
- **`[internal]`** L20 constant audit — compile-time pinned crypto constants across 13 sites — PR #38
- **`[internal]`** CI advisory RUSTSEC-2025-0134 (rustls-pemfile unmaintained) suppressed in `deny.toml`; migration to `rustls-pki-types` deferred to separate backlog — PR #42

### Test coverage

- **`[internal]`** 156 lib tests (incl. 5 `chain::network::tests::*` for `coin_type_for`)
- **`[internal]`** 1 doc test (`compile_fail` for F21 type-level barrier)
- **`[internal]`** Coverage by module: `keys::mnemonic` (mnemonic gen, phrase roundtrip, zeroize), `keys::derivation` (BIP-32 path derivation, hardened/non-hardened), `keys::signer` (sign/verify roundtrip, deterministic per RFC 6979), `crypto::argon2` (key derivation, salt length validation), `crypto::aes_gcm` (roundtrip, password-based end-to-end), `crypto::bip137` (sign/verify, header byte matrix, base64 edge cases, cross-tool interop), `chain::spki` (pin validation), `chain::esplora` (TLS verifier), `chain::network` (BIP-44 coin types, exhaustive match, default = testnet)

### Threat-model coverage

- **`[internal]`** Defended: F1–F13, F19, F20, F21, F25, F26, F34, F37, F43, F44, F47, F48, F50, F53. Deferred: F25 (PSBT review), F48 (IsTerminal), F53 hardening — see plan §Deferred threats.

---

## User Stories

Each story is a user-facing capability. Once checked, the feature is **playaround-able** via the listed test / library call / CLI command. Per L25: flip the checkbox when the merged PR completes the story.

| # | Story | Status | "Try it" |
|---|---|---|---|
| 1 | [x] Generate BIP-39 mnemonic (12/15/18/21/24 words) | done (PR #25) | `cargo test -p bitcoin-wallet-core keys::mnemonic` |
| 2 | [x] Derive child keys via BIP-32 | done (PR #26) | `cargo test -p bitcoin-wallet-core keys::derivation` |
| 3 | [x] Sign + verify messages (BIP-137, Bitcoin Core + Trezor interop) | done (PR #33) | `cargo test -p bitcoin-wallet-core crypto::bip137` |
| 4 | [x] Encrypt with password (Argon2id KDF + AES-256-GCM) | done (PR #27) | `cargo test -p bitcoin-wallet-core crypto::aes_gcm` |
| 5 | [x] Connect to Esplora server (SPKI-pinned TLS, F20) | done (PR #34) | `cargo test -p bitcoin-wallet-core chain::esplora` |
| 6 | [x] Get BIP-44 coin type per network (F37) | done (PR #42) | `cargo test -p bitcoin-wallet-core chain::network` |
| 7 | [x] Compile-time-pinned crypto constants (L20 audit) | done (PR #38) | `cargo test -p bitcoin-wallet-core crypto` (compile-time `const _: ()` blocks fail the build on out-of-range literals) |
| 8 | [x] Refuse mainnet default (CONTEXT.md hard rule #1) | done (PR #42; `bitcoin::Network` has no `Default` impl, so callers must explicitly choose) | see `rust-wallet-app/CONTEXT.md` hard rule #1 |
| 9 | [x] Refuse transaction sighash as message (F21 type-level) | done (PR #39) | `cargo test -p bitcoin-wallet-core --doc threat::MessageHash` |
| 10 | [x] **Create wallet from mnemonic** | done (PR #48, Task 9a) | `cargo test -p bitcoin-wallet-core wallet` |
| 11 | [x] **Sync wallet (full chain scan)** | done (Task 9 #19b.2 + CLI wrapper PR for #63) | Fresh wallet: `cargo test -p bitcoin-wallet-core wallet::tests::sync_completes_against_testnet_for_fresh_wallet -- --ignored --test-threads=1` (requires live testnet Esplora). CLI: `cargo run -p btc -- wallet sync --mnemonic "<12-word phrase>" --network testnet --esplora-url https://blockstream.info/testnet/api` (add `--pin-spki <hex64>` for F20 enforcement). |
| 12 | [x] **Get wallet balance** | done (Task 9 #19c + CLI wrapper PR for #63) | `cargo test -p bitcoin-wallet-core wallet::tests::balance_returns_zero_for_fresh_wallet -- --ignored --test-threads=1` (live testnet). CLI: `cargo run -p btc -- wallet balance --mnemonic "<12-word phrase>" --network testnet --esplora-url https://blockstream.info/testnet/api`. |
| 13 | [x] **Use btc CLI subcommand** | done (PR #70, Task 54d; extended by PR #61, PR #62, PR #73, PR #74, PR for #63) | `cargo run -p btc -- wallet create --words 12 --network testnet` then `cargo run -p btc -- wallet show <wallet_id> --network testnet`; `cargo run -p btc -- message sign --mnemonic "<12-word phrase>" --network testnet --address <ADDR> "x"`; `cargo run -p btc -- encrypt --password hunter2 --in /tmp/plain.txt --out /tmp/cipher.enc` then `btc decrypt --password hunter2 --in /tmp/cipher.enc --out /tmp/recovered.txt`; `cargo run -p btc -- wallet balance --mnemonic "<12-word phrase>" --network testnet --esplora-url https://blockstream.info/testnet/api` |
| 14 | [x] **Send to address** (Story 5 / Issue #118) | done (PR #122) | `cargo run -p btc -- wallet send --mnemonic "<12-word phrase>" --network testnet --address tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx --amount-sat 10000 --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>` (operator-driven per L29; requires funded wallet) |
| 15 | [x] **Custom fee rate** (Story 6 / Issue #119) | done (PR #123) | `cargo run -p btc -- wallet send ... --fee-rate 5` (5 sat/vB); omit `--fee-rate` for default (1 sat/vB) |
| 16 | [x] **Tx history** (Story 7 / Issue #120) | done (PR #125 / this PR) | `cargo run -p btc -- tx-list --mnemonic "<12-word phrase>" --network testnet --esplora-url https://blockstream.info/testnet/api` (operator-driven per L29; requires synced wallet) |
| 17 | [x] **Fee estimates** (Story 8 / Issue #121) | done (PR #124) | `cargo run -p btc -- fee-estimates --network testnet --esplora-url https://blockstream.info/testnet/api` |
| 18 | [x] **Send to multiple recipients** (Story 13 / Issue #138 / sub-task A of #131) | done (PR sub-task A of #131) | `cargo run -p btc -- wallet send --mnemonic "<12-word phrase>" --network testnet --to tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx:10000 --to <addr2>:20000 --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>` (operator-driven per L29; requires funded wallet). Max 20 recipients per tx (BDK safe max). |
| 19 | [x] **Drain wallet to a single address** (Story 14 / Issue #138 / sub-task A of #131) | done (PR sub-task A of #131) | `cargo run -p btc -- wallet send --mnemonic "<12-word phrase>" --network testnet --drain-to tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>` (operator-driven per L29). Add `--exclude-utxo <txid:vout>` (repeatable) to lock specific UTXOs. Add `--dry-run` for signed-PSBT preview (treat as secret). |
| 20 | [x] **Choose coin-selection algorithm** (Story 15 / Issue #139 / sub-task B of #131) | done (PR sub-task B of #131) | `cargo run -p btc -- wallet send --mnemonic "<12-word phrase>" --network testnet --address tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx --amount-sat 10000 --coin-selection knapsack --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>`. Default `bnb` (BDK default); `--coin-selection oldest` for oldest-UTXO-first. |
| 21 | [x] **Manual UTXO selection / coin control** (Story 16 / Issue #139 / sub-task B of #131) | done (PR sub-task B of #131) | `cargo run -p btc -- wallet send --mnemonic "<12-word phrase>" --network testnet --address tb1qw508d6qejxtdg4y5r3zarvary0c5xw7kxpjzsx --amount-sat 10000 --input <txid1>:0 --input <txid2>:1 --manual-selection-only --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>`. Add `--dry-run` to preview the selected UTXO set as a signed PSBT. |
| 22 | [x] **Bump fee (RBF)** (Story 17 / Issue #140 / sub-task C of #131) | done (PR sub-task C of #131) | `cargo run -p btc -- wallet bump-fee --mnemonic "<12-word phrase>" --network testnet --txid <64-hex> --fee-rate <new-sat-per-vb-higher-than-original> --esplora-url https://blockstream.info/testnet/api --pin-spki <hex64>` (operator-driven per L29; requires an unconfirmed tx in the wallet). Add `--dry-run` to preview the RBF replacement as a signed PSBT. Exit 4 if new fee rate does not exceed original. |
| 23 | [x] **Pick address type on creation** (Story 20 / Issue #132) | done (PR #144) | `cargo run -p btc -- wallet create --words 12 --network testnet --type taproot` (default `native-segwit`). Other types: `--type legacy` (BIP-44 P2PKH), `--type nested-segwit` (BIP-49 P2SH-P2WPKH), `--type taproot` (BIP-86 P2TR). `btc wallet show --id <id> --descriptor` rebuilds the correct descriptor from the sidecar metadata file. |

**Progress:** 23 of 23 stories playaround-able.

> **L25 maintenance:** After every PR merge, check if the merged PR completes any unchecked story → flip the box to `[x]` + update the "Try it" column if needed. Drift between docs and actual state is the failure mode this rule prevents (per L14).