# L9 v3 Verdict — #64 PR-1 (lib only; wallet-store persistence layer)

Per L9 v3 schema (per-dimension PASS / PARTIAL / FAIL rubric; from `tasks/lessons.md`).

## Correctness — PASS

- 38 unit tests + 0 failures.
- `end_to_end_encrypt_write_read_decrypt` proves the full pipeline (encrypt → write → read → decrypt).
- `cross_network_aad_mismatch_rejected_at_decrypt` proves N5 footgun closure.
- `read_refuses_symlink_at_blob_path` proves symlink defense (now via BOTH pre-`symlink_metadata` check AND atomic `O_NOFOLLOW` at open(2) — fix from L12 HIGH #2).
- `read_rejects_oversize_blob` proves A2 memory-DoS amplification closure (1 MiB blob rejected).
- `ensure_secure_dir_creates_chain_with_mode_700` + `ensure_secure_dir_refuses_symlink_in_chain` prove F19 atomicity + U6 perms at the boundary.
- `create_wallet_persists_blob_and_returns_id_phrase` + `create_wallet_rejects_unsupported_word_count` + `create_wallet_accepts_supported_word_counts` prove F34 BIP-39 word-count + U6 0o600 file mode.
- `missing_wallet_message_is_indistinguishable` proves N2 oracle collapse on missing-file path (other 3 collapse paths added via L12 HIGH #1 fix; unit-test gap deferred).

## Security — PASS

8 of 8 material L12 findings applied (2 type-design + 4 security + 2 code-reviewer):

1. **security HIGH #2** — `O_NOFOLLOW` flag value was wrong (passed ELOOP errno 40/62 instead of `0o400000`/`0x0100`); fix: separate `o_nofollow_flag()` from `libc_eloop()` + compile-time platform witness via `const _O_NOFOLLOW_PLATFORM_WITNESS`. **This was a real bug from the prior retry that survived PR-68 review**; the security-auditor caught it on the fresh impl.
2. **security HIGH #1** — `ensure_secure_dir` only checked deepest pre-existing ancestor for symlinks; fix: check `p` itself + every dir in the chain.
3. **security MED #1** — `read_to_end` could grow past `MAX_LEN` between stat and read (TOCTOU on file size); fix: `Read::take(max_len)`.
4. **security MED #4** — `peek_addresses` error wrapped bdk content; fix: fixed message per F20 sanitization pattern.
5. **type-design HIGH** — `Default` impl contradicted doc; fix: rewrite doc to acknowledge Default mints fresh v4 (intentional decision from prior session).
6. **type-design MED** — `WalletInfo` missing `#[non_exhaustive]`; fix: added.
7. **code-reviewer HIGH #1** — `show_wallet` only collapsed `Error::Io` to `WALLET_NOT_ACCESSIBLE`, leaking `Error::WalletStore` for symlink + oversize paths; fix: collapse all existence-revealing errors.
8. **code-reviewer HIGH #2** — non-Linux/macOS Unix silently disables `O_NOFOLLOW`; fix: compile-time platform witness.

Threat-model coverage (ADR 0001): F19 / U6 / U7 / A1 / A2 / N2 / N5 / N8 — all defended.

## Test coverage — PARTIAL

- 38 unit tests cover happy path + each oracle surface (N2 collapse on missing-file, N5 cross-network, N8 timing pad duration, U6 perms, U7 layout).
- 3 `#[ignore]` live-Esplora tests deferred to L29 manual smoke.
- **Gap (deferred):** code-reviewer M1+M2 — `peek_addresses` Err path + happy path not unit-tested. Acceptable for PR-1 (Err path tested via `show_wallet`'s missing-wallet integration test; happy path requires a mock `BdkWallet` which is more involved). Filed as v0.1.1 follow-up.
- **Gap (deferred):** code-reviewer M3 — `create_wallet` + `show_wallet` end-to-end roundtrip test not present in PR-1; PR-2 (CLI) adds via integration test.

## Code simplicity — PASS

- Newtype layer minimal: `WalletId(Uuid)` + single `validate()` gate. Constructor + FromStr + TryFrom + Deserialize all route through the same 14-line validator.
- `wallet::ops` is a thin adapter (~120 lines incl. tests): encrypt → write → return, or read → decrypt → wallet.from_mnemonic → sync → balance.
- `wallet::store` is a pure path-computation + atomic-write helper (~330 lines incl. tests).
- `dead_code` warnings on previous attempt (helpers + stub) removed.
- `cargo clippy -p bitcoin-wallet-core --all-targets -- -D warnings` clean.

## Verdict

**PASS** (with PARTIAL on test coverage pending L29 manual smoke + 2 follow-up test gaps). Recommend merge after L9 verdict sign-off.

## What's deferred to PR-2

PR-2 (`btc` CLI subcommands) covers:
- `btc wallet create` + `btc wallet show` clap subcommands
- Manual `Debug` impls redact password (L12 CRITICAL #2 from prior session)
- Step 14 verify-completed gate + step 15c PR-body checklist gate
- L24 cascade (CHANGELOG + README "What's New" + User Story #13 flip)

The PR-1 + PR-2 split means smaller revert surface if #64 retry is reverted again — each PR is independently mergeable.
