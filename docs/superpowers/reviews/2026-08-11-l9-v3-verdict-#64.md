# L9 v3 Verdict — Issue #64 (Task 54d: btc wallet create/show)

Per L9 v3 schema (per-dimension PASS / PARTIAL / FAIL rubric; from `tasks/lessons.md`).

## Correctness — PASS

- 38 unit tests + 4 btc CLI tests, all green.
- `end_to_end_encrypt_write_read_decrypt` proves the full pipeline (generate → encrypt → atomic_write → read → decrypt) recovers the original mnemonic.
- `cross_network_aad_mismatch_rejected_at_decrypt` proves the N5 footgun is closed (testnet AAD + mainnet AAD → AEAD tag failure → `Error::MnemonicCipher`).
- `read_refuses_symlink_at_blob_path` proves the symlink-DoS defense fires (N5 + A2).
- `write_creates_file_with_mode_600` + `ensure_secure_dir_creates_chain_with_mode_700` prove F19 atomicity + U6 perms at the boundary.
- `create_writes_mnemonic_to_stderr_not_stdout` (CLI regression test) proves F49 + L28 invariants end-to-end (wallet_id only on STDOUT; mnemonic only on STDERR with banner).

## Security — PASS

8 of 8 CRITICAL + HIGH findings from L12 review applied:
1. **CRITICAL #1** — `ensure_secure_dir` re-chmods pre-existing wallet root (closes U6 for legacy installs).
2. **CRITICAL #2** — Manual `Debug` impls redact password on `Cli`/`Commands`/`WalletAction` (closes Debug-leak footgun).
3. **HIGH #1** — Manual `Deserialize` routes through `TryFrom<Uuid>` → `validate()` (closes derived-Deserialize bypass).
4. **HIGH #2** — `new_actually_produces_v4_uuid` checks `get_version()` + `get_variant()` (the previous test only checked length).
5. **HIGH #3** — `from_str` now rejects non-RFC-4122 variants (was: only checked version nibble, accepting NCS/Microsoft variants).
6. **HIGH #4** — `O_NOFOLLOW` on read path via `OpenOptions::custom_flags()` (closes TOCTOU window the ADR committed to defending).
7. **HIGH #5** — `constant_time_padding()` on both missing-file AND try_from failure paths (closes N8 timing oracle on all 4 collapse arms).
8. **MED #6** — `read_wallet_at` MAX_LEN cap before allocation (closes A2 memory-DoS amplification).

Threat-model coverage (ADR 0001): F19 / F49 / U6 / U7 / A1 / A2 / N2 / N5 / N8 — all defended.

Remaining LOW/INFO findings (~22) deferred to v0.1.1 hardening (documented in L12 review transcripts).

## Test coverage — PARTIAL

- 38 unit tests cover happy path + each oracle surface + cross-network AAD mismatch + symlink rejection + MAX_LEN rejection + constant-time padding duration.
- 4 btc CLI tests cover clap argument mapping + mnemonic-to-STDERR regression.
- `cargo test` is the standard CI gate.
- **Gap (deferred to L29 manual smoke per #64 acceptance):** live testnet `#[ignore]` tests (`sync_completes_against_testnet_for_fresh_wallet`, `balance_returns_zero_for_fresh_wallet`, `balance_reuses_cached_wallet_on_second_call`) require live network access. The wallet `show` path requires Esplora reachability — full e2e on a real testnet wallet happens in the L29 manual smoke.
- **Gap (deferred to L28 Gate B per ADR §Acceptance):** macOS-specific `data_dir()` path-resolution verification. The code uses `directories::ProjectDirs::from("bt", "btc", "btc")`; the macOS path may need verification that the vendor-prefix doesn't land unexpectedly.

Both gaps are L29/L28 follow-ups, not blockers. The unit-test suite proves the contract is met.

## Code simplicity — PASS

- Newtype layer is minimal: `WalletId(Uuid)` + single `validate()` gate. Constructor + FromStr + TryFrom + Deserialize all route through the same 14-line validator.
- `wallet::ops` is a thin adapter (~120 lines incl. tests): encrypt → write → return, or read → decrypt → wallet.from_mnemonic → sync → balance.
- `wallet::store` is a pure path-computation + atomic-write helper (~330 lines incl. tests). The 0o700 chmod loop is straightforward; the O_NOFOLLOW + ELOOP handling is one syscall wrapper.
- `btc` CLI is ~250 lines: clap subcommands + thin handler functions.
- Dead code (`print_wallet_info`, `validate_words_supported`, `override_base` stub) removed per L12 code-review HIGH #3.
- `cargo clippy -p bitcoin-wallet-core -p btc --all-targets -- -D warnings` clean.

## Verdict

**PASS** (with PARTIAL on test coverage pending L29 manual smoke + macOS data_dir verification). Recommend merge after L29 smoke test against live testnet (operator-driven, ~5 min).
