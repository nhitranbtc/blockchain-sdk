.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.metadata

# Prebuilt FFI shared library (built by `cargo build` against bitcoin-wallet-core)
# Source repo stays slim — regenerate via build instructions in runner/.
native/
*.iml
.idea/
.vscode/
.fvm/
coverage/
*.log

# Test fixture runtime artifacts — the fake_btc.sh fixture writes its
# inherited (filtered) env to `.last_env` so the L7 env-strip test can
# verify BtcInvoker's filter. Per-run; never commit.
test/integration/fixtures/.last_env
